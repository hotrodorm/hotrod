<?xml version="1.0"?>
<!DOCTYPE select>

<!-- 10. Recursive Tags -->

<select name="searchedAccount" vo="AccountVO">

  select * from account
  <if test="conditional">
    where
    <if test="state">
      state = 'active'
      <!-- 
       -->
      <when>abc</when>
      <!-- 
      <choose>
        <when test="large">
          and size = 'L'
        </when>
        <when test="small">
          and size = 'S'
        </when>
        <otherwise>
          and size = 'M'
        </otherwise>
      </choose>
         -->
    </if>
    <if test="current_balance">
      balance between 100 and 200
    </if>
  </if>

</select>
