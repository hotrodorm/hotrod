# Highlights of HotRod 5 ORM

As developers ourselves we started HotRod when we decided that we didn't like any of the available ORM solutions at the time.

The initial goal of HotRod was to speed up the development of phase an app. This included quick prototyping and also enhance the support for the gradual growth of the apps. That is, when a project just starts schema discovery gives an edge to the team; then, when the projects grows, LiveSQL, Dynamic SQL, and Nitro can implement complex functionality with little effort.

Later on, we realized we also wanted to reduce the operational cost of the apps. Apart from the coding phase we decided to consider the debugging, testing, support, maintenance, and above all the stability of the apps themselves. That's where Torcs, SQL Injection safety, API simplification, compile-time syntax checks, and many others features came into play.


## Top-of-the-Line ORM Features

The list of top of the line ORM features that we don't find in the competitors. We believe HotRod shines with these features:

1. Schema Discovery that Generate a Full Persistence Layer in Minutes
- Out-of-the-box CRUD for Quick Prototyping and More
- Feature-Rich LiveSQL including Tuples and Meta-Expressions
- Advanced Type Resolution Mechanics for Column Types
- Layout &amp; Model Entities
- Out-of-the-box Cursors to Stream Data from Selects
- Detection of Slow SQL Performance and Retrieval of Query Execution Plans
- Dynamic SQL queries that change shape according to the runtime parameters
- Multiple Optimistic Locking Strategies in CRUD

## World-Class ORM Features

The list of world-class ORM features includes what you would expect from any typical world-class ORM:

1. Supports for world-class OLTP databases and in-memory databases
1. Supports back from Java 25 back to 8 and Spring Boot 3.x back to 2.x
1. No-Config Mode for quick prototyping
1. Multi-datasource Aware
- Feature-Rich LiveSQL SQL syntax, clauses, and expressions
- LiveSQL queries are syntax-checked while writing them
- Fully SQL Injection Safe
- LiveSQL Subqueries, CTEs, and Recursive CTEs
- LiveSQL Self-Joins, Semi-Joins, and Reflexive Joins
- LiveSQL General Functions, Window Functions, and Custom Functions
- Advanced DynamicSQL
- Slow SQL Performance Detection Using Torcs
- Feature-Full Configuration
- Configuration Partitioning for Large Apps
- Flexible Entity and methods naming
- Flexible organization of the persistence layer
- Native SQL
- Read and Write Casting and Data Conversion
- Auto-generated identity and sequence-based primary keys
- Advanced CRUD Complex Predicates
- Selecting from Sequences
- Row Locking for Thread-Safe Operations
- Previewing of Queries
- Configurable SQL Logging
- Simple and Composite Keys
