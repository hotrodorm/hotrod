# Support for H2 Database

HotRod includes the H2 database dialect that supports the generation of the persistence layer, and also at runtime when running SQL queries in it.

The H2 dialect includes a set of built-in rules that define a sraightforward way of mapping common database column types to application types. These built-in rules are used by default and do not require any custom configuration to be activated.

They appear in third precedence in the Type Resolution Mechanism, after the *Designated* and *Type Solver* rules. See [Type Resolution Mechanism](../type-resolution-mechanism.md) to customize the rules and to use their full precedence.


## 1. Static Dialect Rules

The Static Dialect Rules are used to compute the types of properties used in the application to retrieve data from the database. These types are computed during the generation of the persistence layer; the types decided at this opportunity are set in stone and cannot be changed at runtime.

The following table shows rules that are used to map the column types in the database to the types you see in your app.

The rules are denoted using the symbol :small_orange_diamond: followed by the rule ID; for example, "D4" means "Static Dialect Rule #4". The rule IDs are included as comments in the properties of the layout classes, and also in the LiveSQL queries (when logging). They describe which rule exactly was used to compute the data type for a column.

| H2 Column Type | Default Java Type |
| -- | -- |
| `DECIMAL(p,s)`,<br/>`DEC(p,s)`,<br/>`NUMERIC(p,s)`,<br/>`NUMBER(p,s)` | If s is specified and different from zero the type is:<br/>&nbsp;&nbsp;&bull; `java.math.BigDecimal`:small_orange_diamond:D1<br/>if s is not specified or specified with a value of zero:<br/>&nbsp;&nbsp;&bull; if p <= 2: `java.lang.Byte`:small_orange_diamond:D2<br/>&nbsp;&nbsp;&bull; if 2 < p <= 4: `java.lang.Short`:small_orange_diamond:D3<br/>&nbsp;&nbsp;&bull; if 4 < p <= 9: `java.lang.Integer`:small_orange_diamond:D4<br/>&nbsp;&nbsp;&bull; if 8 < p <= 18: `java.lang.Long`:small_orange_diamond:D5<br/>&nbsp;&nbsp;&bull; if p > 18: `java.math.BigInteger`:small_orange_diamond:D6 |
| `TINYINT` | `java.lang.Byte` :small_orange_diamond:D7|
| `SMALLINT`,<br/>`INT2` | `java.lang.Short`:small_orange_diamond:D8 |
| `INTEGER`,<br/>`INT`,<br/>`MEDIUMINT`,<br/>`INT4`,<br/>`SIGNED`,<br/>`IDENTITY` | `java.lang.Integer`:small_orange_diamond:D9 |
| `BIGINT`,<br/>`INT8` | `java.lang.Long`:small_orange_diamond:D10|
| `DOUBLE`,<br/>`DOUBLE PRECISION`,<br/>`FLOAT`,<br/>`FLOAT8` | `java.lang.Double`:small_orange_diamond:D11 |
| `REAL`,<br/>`FLOAT4` | `java.lang.Float`:small_orange_diamond:D12|
| `CHAR(n)`,<br/>`CHARACTER(n)`,<br/>`NCHAR(n)` | `java.lang.String`:small_orange_diamond:D13 |
| `VARCHAR(n)`,<br/>`LONGVARCHAR(n)`,<br/>`VARCHAR2(n)`,<br/>`NVARCHAR(n)`,<br/>`NVARCHAR2(n)`,<br/>`VARCHAR_CASEINSENSITIVE(n)`,<br/>`VARCHAR_INGNORECASE(n)` | `java.lang.String`:small_orange_diamond:D14 |
| `CLOB(n)`,<br/>`NCLOB(n)`,<br/>`TINYTEXT(n)`,<br/>`TEXT(n)`,<br/>`MEDIUMTEXT(n)`,<br/>`LONGTEXT(n)`,<br/>`NTEXT(n)` | `java.lang.String`:small_orange_diamond:D15 [^1] |
| `DATE` | `java.sql.Date`:small_orange_diamond:D16 |
| `TIME` | `java.sql.Time`:small_orange_diamond:D17 |
| `TIMESTAMP`,<br/>`DATETIME`,<br/>`SMALLDATETIME` | `java.time.LocalDateTime`:small_orange_diamond:D18 |
| `TIMESTAMP WITH TIME ZONE` | `java.time.OffsetDateTime`:small_orange_diamond:D23 |
| `VARBINARY(n)`,<br/>`LONGVARBINARY(n)` | `byte[]`:small_orange_diamond:D19 |
| `RAW(n)`,<br/>`BYTEA(n)`,<br/>`BLOB(n)`,<br/>`TINYBLOB(n)`,<br/>`MEDIUMBLOB(n)`,<br/>`LONGBLOB(n)`,<br/>`IMAGE(n)`,<br/>`OID(n)` | `byte[]`:small_orange_diamond:D20 [^1] |
| `BOOLEAN`,<br/>`BIT`,<br/>`BOOL` | `java.lang.Boolean`:small_orange_diamond:D21 |
| `BINARY(n)` | `byte[]`:small_orange_diamond:D22 |
| `ARRAY` | No default HotRod data type [^2] |
| `GEOMETRY` | No default HotRod data type [^2] |


## 2. Runtime Dialect Rules

The Runtime Dialect Rules are used to compute the types of LiveSQL expressions when retrieving data from the database using SELECT queries.

These rules are not used if a LiveSQL was explicitly designated a type or converter using `.type(<class>)`; they are not used either if a Runtime Type Solver rule matched this column and decided on the column type or converter. These two have precedence over the Runtime Dialect Rules.

These rules affect only LiveSQL expression; LiveSQL table or view columns &ndash; such as `a.balance` where `a` is a table or view &ndash; are not affected since their types were already computed in a static manner during the persistence layer generation.

The following table shows rules that are used to map the column types in the database to the types you see in your app.

The rules are denoted using the symbol :small_orange_diamond: followed by the rule ID; for example, "RD3" means "Runtime Dialect Rule #3". The rule IDs are shown in the LiveSQL queries (when logging). They describe which rule exactly was used to compute the data type for a column.

| H2 Column Type | Default Java Type |
| -- | -- |
| `DECIMAL(p,s)`,<br/>`DEC(p,s)`,<br/>`NUMERIC(p,s)`,<br/>`NUMBER(p,s)` | If s is specified and different from zero the type is:<br/>&nbsp;&nbsp;&bull; `java.math.BigDecimal`:small_orange_diamond:RD1<br/>if s is not specified or specified with a value of zero:<br/>&nbsp;&nbsp;&bull; if p <= 2: `java.lang.Byte`:small_orange_diamond:RD2<br/>&nbsp;&nbsp;&bull; if 2 < p <= 4: `java.lang.Short`:small_orange_diamond:RD3<br/>&nbsp;&nbsp;&bull; if 4 < p <= 9: `java.lang.Integer`:small_orange_diamond:RD4<br/>&nbsp;&nbsp;&bull; if 8 < p <= 18: `java.lang.Long`:small_orange_diamond:RD5<br/>&nbsp;&nbsp;&bull; if p > 18: `java.math.BigInteger`:small_orange_diamond:RD6 |
| `TINYINT` | `java.lang.Byte` :small_orange_diamond:RD7|
| `SMALLINT`,<br/>`INT2` | `java.lang.Short`:small_orange_diamond:RD8 |
| `INTEGER`,<br/>`INT`,<br/>`MEDIUMINT`,<br/>`INT4`,<br/>`SIGNED`,<br/>`IDENTITY` | `java.lang.Integer`:small_orange_diamond:RD9 |
| `BIGINT`,<br/>`INT8` | `java.lang.Long`:small_orange_diamond:RD10|
| `DOUBLE`,<br/>`DOUBLE PRECISION`,<br/>`FLOAT`,<br/>`FLOAT8` | `java.lang.Double`:small_orange_diamond:RD11 |
| `REAL`,<br/>`FLOAT4` | `java.lang.Float`:small_orange_diamond:RD12|
| `CHAR(n)`,<br/>`CHARACTER(n)`,<br/>`NCHAR(n)` | `java.lang.String`:small_orange_diamond:RD13 |
| `VARCHAR(n)`,<br/>`LONGVARCHAR(n)`,<br/>`VARCHAR2(n)`,<br/>`NVARCHAR(n)`,<br/>`NVARCHAR2(n)`,<br/>`VARCHAR_CASEINSENSITIVE(n)`,<br/>`VARCHAR_IGNORECASE(n)` | `java.lang.String`:small_orange_diamond:RD14 |
| `CLOB(n)`,<br/>`NCLOB(n)`,<br/>`TINYTEXT(n)`,<br/>`TEXT(n)`,<br/>`MEDIUMTEXT(n)`,<br/>`LONGTEXT(n)`,<br/>`NTEXT(n)` | `java.lang.String`:small_orange_diamond:RD15 [^1] |
| `DATE` | `java.sql.Date`:small_orange_diamond:RD16 |
| `TIME` | `java.sql.Time`:small_orange_diamond:RD17 |
| `TIME WITH TIME ZONE` | `java.sql.Time`:small_orange_diamond:RD18 |
| `TIMESTAMP`,<br/>`DATETIME`,<br/>`SMALLDATETIME` | `java.time.LocalDateTime`:small_orange_diamond:RD19 |
| `TIMESTAMP WITH TIME ZONE` | `java.time.OffsetDateTime`:small_orange_diamond:RD20 |
| `VARBINARY(n)`,<br/>`VARBINARY(n)`,<br/>`LONGVARBINARY(n)`,<br/>`RAW(n)`,<br/>`BYTEA(n)` | `byte[]`:small_orange_diamond:RD21 |
| `BLOB(n)`,<br/>`TINYBLOB(n)`,<br/>`MEDIUMBLOB(n)`,<br/>`LONGBLOB(n)`,<br/>`IMAGE(n)` | `byte[]`:small_orange_diamond:RD22 [^1] |
| `BINARY(n)` | `byte[]`:small_orange_diamond:RD23 |
| `BOOLEAN`,<br/>`BIT`,<br/>`BOOL` | `java.lang.Boolean`:small_orange_diamond:RD24 |

[^1]: LOB types are by default read all at once into memory as byte arrays. They can also be read/written using streaming instead of loading them all at once. To do this you’ll need to write a `<converter>`.

[^2]: To use this column you can designate a specific type or specific converter for it using the `<column>` tag. Alternatively you can define a static Type Solver rule to define the type or converter to use for it.

