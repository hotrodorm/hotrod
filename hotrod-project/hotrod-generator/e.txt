
DONE 1. [alta] Operaciones de "insert" retornan 'null' en lugar del objeto-model insertado.

DONE 2. [alta] Operaciones de "insert" reciben como parámetro un objeto-model en lugar de un objeto-layout. (Está relacionado con el punto anterior).

3. [alta] Operaciones de "update" retornan 'null' en lugar de un número que representa la cantidad de filas afectadas.

4. [media] Tag <sequence> fue removido en la versión 5. El workaround de obtener un 'sequence' a través de un tag <select> obliga a modificar 
          el código fuente (para sacar una propiedad dentro del objeto generado en lugar del dato primitivo como retorno).

5. [media] Restaurar el "Predicate" (marcado deprecado)

6. [media] Restaurar "parseRow" (marcado deprecado)

7. [media] Operaciones de modificación lanzan Exception en lugar de RuntimeException, lo que, en procesos de migración, obliga a modificar código fuente. 

8. [baja] No es posible configurar el generador, para obtener la misma estructura de 'packages' de las clases generadas, que aquellos generados por la
          versión 4. Ejemplo: "model" para las clases Model y "model.implementation" para las clases Layout.
          
                    