// Autogenerated by HotRod -- Do not edit.

package test.persistence.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import test.persistence.primitives.Quadrant;
import test.persistence.QuadrantVO;
import test.persistence.CodesVO;
import test.persistence.primitives.CodesDAO.CodesOrderBy;
import test.persistence.primitives.CodesDAO;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.SQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class QuadrantDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Lazy
  @Autowired
  private CodesDAO codesDAO;

  @Autowired
  private SQLDialect sqlDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public test.persistence.QuadrantVO selectByPK(final java.lang.Integer region, final java.lang.Integer area) {
    if (region == null)
      return null;
    if (area == null)
      return null;
    test.persistence.QuadrantVO vo = new test.persistence.QuadrantVO();
    vo.setRegion(region);
    vo.setArea(area);
    return this.sqlSession.selectOne("test.persistence.primitives.quadrant.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<test.persistence.QuadrantVO> selectByExample(final test.persistence.primitives.Quadrant example, final QuadrantOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.primitives.Quadrant, QuadrantOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("test.persistence.primitives.quadrant.selectByExample", dwo);
  }

  public Cursor<test.persistence.QuadrantVO> selectByExampleCursor(final test.persistence.primitives.Quadrant example, final QuadrantOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.primitives.Quadrant, QuadrantOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<test.persistence.QuadrantVO>(this.sqlSession.selectCursor("test.persistence.primitives.quadrant.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<test.persistence.QuadrantVO> selectByCriteria(final QuadrantDAO.QuadrantTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<test.persistence.QuadrantVO>(from, this.sqlDialect, this.sqlSession,
        predicate, "test.persistence.primitives.quadrant.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs

  public SelectChildrenCodesPhase selectChildrenCodesOf(final QuadrantVO vo) {
    return new SelectChildrenCodesPhase(vo);
  }

  public class SelectChildrenCodesPhase {

    private QuadrantVO vo;

    SelectChildrenCodesPhase(final QuadrantVO vo) {
      this.vo = vo;
    }

    public SelectChildrenCodesFromRegionAreaPhase fromRegionArea() {
      return new SelectChildrenCodesFromRegionAreaPhase(this.vo);
    }

  }

  public class SelectChildrenCodesFromRegionAreaPhase {

    private QuadrantVO vo;

    SelectChildrenCodesFromRegionAreaPhase(final QuadrantVO vo) {
      this.vo = vo;
    }

    public List<CodesVO> toAccount_VersionName(final CodesOrderBy... orderBies) {
      CodesVO example = new CodesVO();
      example.setAccount(this.vo.getRegion());
      example.setVersionName(this.vo.getArea());
      return codesDAO.selectByExample(example, orderBies);
    }

    public Cursor<CodesVO> cursorToAccount_VersionName(final CodesOrderBy... orderBies) {
      CodesVO example = new CodesVO();
      example.setAccount(this.vo.getRegion());
      example.setVersionName(this.vo.getArea());
      return codesDAO.selectByExampleCursor(example, orderBies);
    }

    public List<CodesVO> toAccountVersion_Name(final CodesOrderBy... orderBies) {
      CodesVO example = new CodesVO();
      example.setAccountVersion(this.vo.getRegion());
      example.setName(this.vo.getArea());
      return codesDAO.selectByExample(example, orderBies);
    }

    public Cursor<CodesVO> cursorToAccountVersion_Name(final CodesOrderBy... orderBies) {
      CodesVO example = new CodesVO();
      example.setAccountVersion(this.vo.getRegion());
      example.setName(this.vo.getArea());
      return codesDAO.selectByExampleCursor(example, orderBies);
    }

  }

  // insert

  public test.persistence.QuadrantVO insert(final test.persistence.primitives.Quadrant vo) {
    String id = "test.persistence.primitives.quadrant.insert";
    this.sqlSession.insert(id, vo);
    test.persistence.QuadrantVO mo = new test.persistence.QuadrantVO();
    mo.setRegion(vo.getRegion());
    mo.setArea(vo.getArea());
    mo.setCaption(vo.getCaption());
    return mo;
  }

  // update by PK

  public int update(final test.persistence.QuadrantVO vo) {
    if (vo.region == null) return 0;
    if (vo.area == null) return 0;
    return this.sqlSession.update("test.persistence.primitives.quadrant.updateByPK", vo);
  }

  // delete by PK

  public int delete(final test.persistence.QuadrantVO vo) {
    if (vo.region == null) return 0;
    if (vo.area == null) return 0;
    return this.sqlSession.delete("test.persistence.primitives.quadrant.deleteByPK", vo);
  }

  // update by example

  public int updateByExample(final test.persistence.primitives.Quadrant example, final test.persistence.primitives.Quadrant updateValues) {
    UpdateByExampleDao<test.persistence.primitives.Quadrant> fvd = //
      new UpdateByExampleDao<test.persistence.primitives.Quadrant>(example, updateValues);
    return this.sqlSession.update("test.persistence.primitives.quadrant.updateByExample", fvd);
  }

  // delete by example

  public int deleteByExample(final test.persistence.primitives.Quadrant example) {
    return this.sqlSession.delete("test.persistence.primitives.quadrant.deleteByExample", example);
  }

  // DAO ordering

  public enum QuadrantOrderBy implements OrderBy {

    REGION("quadrant", "region", true), //
    REGION$DESC("quadrant", "region", false), //
    AREA("quadrant", "area", true), //
    AREA$DESC("quadrant", "area", false), //
    CAPTION("quadrant", "caption", true), //
    CAPTION$DESC("quadrant", "caption", false), //
    CAPTION$CASEINSENSITIVE("quadrant", "lower(caption)", true), //
    CAPTION$CASEINSENSITIVE_STABLE_FORWARD("quadrant", "lower(caption), caption", true), //
    CAPTION$CASEINSENSITIVE_STABLE_REVERSE("quadrant", "lower(caption), caption", false), //
    CAPTION$DESC_CASEINSENSITIVE("quadrant", "lower(caption)", false), //
    CAPTION$DESC_CASEINSENSITIVE_STABLE_FORWARD("quadrant", "lower(caption), caption", false), //
    CAPTION$DESC_CASEINSENSITIVE_STABLE_REVERSE("quadrant", "lower(caption), caption", true);

    private QuadrantOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static QuadrantTable newTable() {
    return new QuadrantTable();
  }

  public static QuadrantTable newTable(final String alias) {
    return new QuadrantTable(alias);
  }

  public static class QuadrantTable extends Table {

    // Properties

    public NumberColumn region;
    public NumberColumn area;
    public StringColumn caption;

    // Constructors

    QuadrantTable() {
      super(null, null, "quadrant", "Table", null);
      initialize();
    }

    QuadrantTable(final String alias) {
      super(null, null, "quadrant", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.region = new NumberColumn(this, "region", "region");
      this.area = new NumberColumn(this, "area", "area");
      this.caption = new StringColumn(this, "caption", "caption");
    }

  }

}
