// Autogenerated by HotRod -- Do not edit.

package test.persistence.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import test.persistence.HouseVO;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.SQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class HouseDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private SQLDialect sqlDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<test.persistence.HouseVO> selectByExample(final test.persistence.HouseVO example, final HouseOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.HouseVO, HouseOrderBy> dwo = //
        new DaoWithOrder<test.persistence.HouseVO, HouseOrderBy>(example, orderBies);
    return this.sqlSession.selectList("test.persistence.primitives.house.selectByExample", dwo);
  }

  public Cursor<test.persistence.HouseVO> selectByExampleCursor(final test.persistence.HouseVO example, final HouseOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.HouseVO, HouseOrderBy> dwo = //
        new DaoWithOrder<test.persistence.HouseVO, HouseOrderBy>(example, orderBies);
    return new MyBatisCursor<test.persistence.HouseVO>(this.sqlSession.selectCursor("test.persistence.primitives.house.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<test.persistence.HouseVO> selectByCriteria(final HouseDAO.HouseTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<test.persistence.HouseVO>(from, this.sqlDialect, this.sqlSession,
        predicate, "test.persistence.primitives.house.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public int insert(final test.persistence.HouseVO vo) {
    String id = "test.persistence.primitives.house.insert";
    return this.sqlSession.insert(id, vo);
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int updateByExample(final test.persistence.HouseVO example, final test.persistence.HouseVO updateValues) {
    UpdateByExampleDao<test.persistence.HouseVO> fvd = //
      new UpdateByExampleDao<test.persistence.HouseVO>(example, updateValues);
    return this.sqlSession.update("test.persistence.primitives.house.updateByExample", fvd);
  }

  // delete by example

  public int deleteByExample(final test.persistence.HouseVO example) {
    return this.sqlSession.delete("test.persistence.primitives.house.deleteByExample", example);
  }

  // DAO ordering

  public enum HouseOrderBy implements OrderBy {

    ADDRESS("house", "address", true), //
    ADDRESS$DESC("house", "address", false), //
    ADDRESS$CASEINSENSITIVE("house", "lower(address)", true), //
    ADDRESS$CASEINSENSITIVE_STABLE_FORWARD("house", "lower(address), address", true), //
    ADDRESS$CASEINSENSITIVE_STABLE_REVERSE("house", "lower(address), address", false), //
    ADDRESS$DESC_CASEINSENSITIVE("house", "lower(address)", false), //
    ADDRESS$DESC_CASEINSENSITIVE_STABLE_FORWARD("house", "lower(address), address", false), //
    ADDRESS$DESC_CASEINSENSITIVE_STABLE_REVERSE("house", "lower(address), address", true), //
    PRICE("house", "price", true), //
    PRICE$DESC("house", "price", false);

    private HouseOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static HouseTable newTable() {
    return new HouseTable();
  }

  public static HouseTable newTable(final String alias) {
    return new HouseTable(alias);
  }

  public static class HouseTable extends Table {

    // Properties

    public StringColumn address;
    public NumberColumn price;

    // Constructors

    HouseTable() {
      super(null, null, "house", "Table", null);
      initialize();
    }

    HouseTable(final String alias) {
      super(null, null, "house", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.address = new StringColumn(this, "address", "address");
      this.price = new NumberColumn(this, "price", "price");
    }

  }

}
