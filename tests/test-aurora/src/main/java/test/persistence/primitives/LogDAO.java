// Autogenerated by HotRod -- Do not edit.

package test.persistence.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import test.persistence.primitives.Log;
import test.persistence.LogVO;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.SQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class LogDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private SQLDialect sqlDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<test.persistence.LogVO> selectByExample(final test.persistence.primitives.Log example, final LogOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.primitives.Log, LogOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("test.persistence.primitives.log.selectByExample", dwo);
  }

  public Cursor<test.persistence.LogVO> selectByExampleCursor(final test.persistence.primitives.Log example, final LogOrderBy... orderBies)
      {
    DaoWithOrder<test.persistence.primitives.Log, LogOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<test.persistence.LogVO>(this.sqlSession.selectCursor("test.persistence.primitives.log.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<test.persistence.LogVO> selectByCriteria(final LogDAO.LogTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<test.persistence.LogVO>(from, this.sqlDialect, this.sqlSession,
        predicate, "test.persistence.primitives.log.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public test.persistence.LogVO insert(final test.persistence.primitives.Log vo) {
    String id = "test.persistence.primitives.log.insert";
    this.sqlSession.insert(id, vo);
    test.persistence.LogVO mo = new test.persistence.LogVO();
    mo.setRecordedAt(vo.getRecordedAt());
    mo.setNotes(vo.getNotes());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int updateByExample(final test.persistence.primitives.Log example, final test.persistence.primitives.Log updateValues) {
    UpdateByExampleDao<test.persistence.primitives.Log> fvd = //
      new UpdateByExampleDao<test.persistence.primitives.Log>(example, updateValues);
    return this.sqlSession.update("test.persistence.primitives.log.updateByExample", fvd);
  }

  // delete by example

  public int deleteByExample(final test.persistence.primitives.Log example) {
    return this.sqlSession.delete("test.persistence.primitives.log.deleteByExample", example);
  }

  // DAO ordering

  public enum LogOrderBy implements OrderBy {

    RECORDED_AT("log", "recorded_at", true), //
    RECORDED_AT$DESC("log", "recorded_at", false), //
    NOTES("log", "notes", true), //
    NOTES$DESC("log", "notes", false), //
    NOTES$CASEINSENSITIVE("log", "lower(notes)", true), //
    NOTES$CASEINSENSITIVE_STABLE_FORWARD("log", "lower(notes), notes", true), //
    NOTES$CASEINSENSITIVE_STABLE_REVERSE("log", "lower(notes), notes", false), //
    NOTES$DESC_CASEINSENSITIVE("log", "lower(notes)", false), //
    NOTES$DESC_CASEINSENSITIVE_STABLE_FORWARD("log", "lower(notes), notes", false), //
    NOTES$DESC_CASEINSENSITIVE_STABLE_REVERSE("log", "lower(notes), notes", true);

    private LogOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static LogTable newTable() {
    return new LogTable();
  }

  public static LogTable newTable(final String alias) {
    return new LogTable(alias);
  }

  public static class LogTable extends Table {

    // Properties

    public DateTimeColumn recordedAt;
    public StringColumn notes;

    // Constructors

    LogTable() {
      super(null, null, "log", "Table", null);
      initialize();
    }

    LogTable(final String alias) {
      super(null, null, "log", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.recordedAt = new DateTimeColumn(this, "recorded_at", "recordedAt");
      this.notes = new StringColumn(this, "notes", "notes");
    }

  }

}
