// Autogenerated by HotRod -- Do not edit.

package app.persistence.dao.test4;

import java.io.Serializable;
import java.sql.Connection;
import java.sql.Date;
import java.sql.ResultSet;
import java.sql.ResultSetMetaData;
import java.sql.SQLException;
import java.sql.Types;
import java.time.LocalDate;
import java.util.List;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.dynamicsql.DynamicExpressionException;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.Parameters;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.RowReader;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.livesql.LShield;
import org.hotrod.livesql.LiveSQL;
import org.hotrod.livesql.dialects.LiveSQLDialect;
import org.hotrod.livesql.queries.LiveSQLContext;
import org.hotrod.livesql.queries.typesolver.TypeSolver;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.stereotype.Component;

import app.persistence.model.test4.FindTareasPorFiltro;

@Component
public class Test4Finder implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  private static final Logger log = Logger.getLogger(Test4Finder.class.getName());

  @Autowired
  private DataSource dataSource;

  @SuppressWarnings("unused")
  @Autowired
  private LiveSQL sql;

  private DynamicSQL dyn;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  @SuppressWarnings("unused")
  private LiveSQLContext context;

  // NITRO SELECT: findTareasPorFiltro

  private DynamicSelectQuery select0;

  private void initializeSelect0() {
    this.select0 = dyn
      .literal("\n      ")
      .literal("\n\nSELECT *\nFROM invoice\nWHERE\n")
      .if_("fechaDesde != null")
        .literal("\norder_date < ")
        .parameterNullable("fechaDesde", Types.DATE)
        .literal(" AND\n")
      .endif()
      .literal("\namount > 50\n\n    ")
      .endSelectQuery();
  }

  public final class RowReader0 implements RowReader<FindTareasPorFiltro> {

    private boolean present1 = false;
    private boolean present2 = false;
    private boolean present3 = false;
    private boolean present4 = false;
    private boolean present5 = false;
    private boolean present6 = false;
    private boolean present7 = false;
    private boolean present8 = false;

    @Override
    public void discoverColumns(ResultSet rs) throws SQLException {
      ResultSetMetaData m = rs.getMetaData();
      present1 = false;
      present2 = false;
      present3 = false;
      present4 = false;
      present5 = false;
      present6 = false;
      present7 = false;
      present8 = false;
      int n = m.getColumnCount();
      for (int i = 1; i <= n; i++) {
        String l = m.getColumnLabel(i);
        if ("ID".equals(l)) present1 = true;
        if ("ACCOUNT_ID".equals(l)) present2 = true;
        if ("AMOUNT".equals(l)) present3 = true;
        if ("BRANCH_ID".equals(l)) present4 = true;
        if ("ORDER_DATE".equals(l)) present5 = true;
        if ("TYPE".equals(l)) present6 = true;
        if ("UNPAID_BALANCE".equals(l)) present7 = true;
        if ("STATUS".equals(l)) present8 = true;
      }
    }

    @Override
    public FindTareasPorFiltro readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      FindTareasPorFiltro row = applicationContext.getBean(FindTareasPorFiltro.class);

      if (this.present1) {
        Integer col1 = rs.getInt("ID"); // ID
        if (rs.wasNull()) col1 = null;
        row.setId(col1);
      }

      if (this.present2) {
        Integer col2 = rs.getInt("ACCOUNT_ID"); // ACCOUNT_ID
        if (rs.wasNull()) col2 = null;
        row.setAccountId(col2);
      }

      if (this.present3) {
        Integer col3 = rs.getInt("AMOUNT"); // AMOUNT
        if (rs.wasNull()) col3 = null;
        row.setAmount(col3);
      }

      if (this.present4) {
        Integer col4 = rs.getInt("BRANCH_ID"); // BRANCH_ID
        if (rs.wasNull()) col4 = null;
        row.setBranchId(col4);
      }

      if (this.present5) {
        LocalDate col5 = rs.getObject("ORDER_DATE", LocalDate.class); // ORDER_DATE
        row.setOrderDate(col5);
      }

      if (this.present6) {
        String col6 = rs.getString("TYPE"); // TYPE
        row.setType(col6);
      }

      if (this.present7) {
        Integer col7 = rs.getInt("UNPAID_BALANCE"); // UNPAID_BALANCE
        if (rs.wasNull()) col7 = null;
        row.setUnpaidBalance(col7);
      }

      if (this.present8) {
        String col8 = rs.getString("STATUS"); // STATUS
        row.setStatus(col8);
      }

      return row;
    }

  };
  public List<FindTareasPorFiltro> findTareasPorFiltro(Date fechaDesde) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("fechaDesde", fechaDesde);
    RowReader0 rr = new RowReader0();
    PreparedSelectQuery<FindTareasPorFiltro> preparedQuery = this.select0.prepare(context, rr);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<FindTareasPorFiltro> rows = preparedQuery.execute(conn);
      return rows;
    }
  }

  @PostConstruct
  public void initializeContext() {
    LiveSQLDialect liveSQLDialect = LShield.getLiveSQLDialect(this.sql);
    this.context = new LiveSQLContext(liveSQLDialect, this.dataSource, new TypeSolver(null, liveSQLDialect), log);
    this.dyn = new DynamicSQL();
    this.initializeSelect0();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finest("SQL:\n" + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL:\n" + preparedQuery.getPreview());
    }
  }

  public DataSource getDataSource() {
    return this.dataSource;
  }

}
