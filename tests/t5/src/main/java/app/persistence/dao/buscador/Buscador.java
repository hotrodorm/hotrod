// Autogenerated by HotRod -- Do not edit.

package app.persistence.dao.buscador;

import java.io.Serializable;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.data.RowReader;
import org.hotrod.dynamicsql.DynamicExpressionException;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.ParameterContext;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.runtime.livesql.LiveSQL;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.queries.LiveSQLContext;
import org.hotrod.runtime.livesql.queries.typesolver.TypeSolver;
import org.hotrod.runtime.livesql.util.DynamicSQLBean;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.stereotype.Component;

import app.persistence.model.buscador.FindTareasPorFiltro;

@Component
public class Buscador implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  private static final Logger log = Logger.getLogger(Buscador.class.getName());

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  @Autowired
  private DynamicSQLBean dynamicSQLBean;

  private DynamicSQL dyn;

  @Autowired
  private DataSource dataSource;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  @Autowired
  private LiveSQL sql;

  private LiveSQLContext context;

  // NITRO SELECT: findTareasPorFiltro

  private DynamicSelectQuery select0;

  private void initializeSelect0() {
    this.select0 = dyn
      .literal("\n\n      ")
      .literal("\n      ")
      .literal("\n\n      select *\n      from a\n      ")
      .literal("\n        ")
      .if_("idArea != null", dyn
        .literal("\n        left join b on b.aid = a.id\n          ")
        .if_("idSector != null", dyn
          .literal("\n            where b.val is not null\n          ")
          .end()
        )
        .literal("\n        ")
        .end()
      )
      .literal("\n      ")
      .literal("\n\n    ")
      .endSelectQuery();
  }


  private final RowReader<FindTareasPorFiltro> rowReader0 = new RowReader<FindTareasPorFiltro>() {

    @Override
    public FindTareasPorFiltro readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      FindTareasPorFiltro row = applicationContext.getBean(FindTareasPorFiltro.class);

      Integer col1 = rs.getInt(1); // ID
      if (rs.wasNull()) col1 = null;
      row.setId(col1);

      String col2 = rs.getString(2); // NAME
      row.setName(col2);

      return row;
    }

  };
  public List<FindTareasPorFiltro> findTareasPorFiltro(Long idArea, Long idSector) throws DynamicExpressionException, SQLException {
    ParameterContext context = this.dyn.newParameterContext();
    context.add("idArea", idArea);
    context.add("idSector", idSector);
    PreparedSelectQuery<FindTareasPorFiltro> preparedQuery = this.select0.prepare(context, this.rowReader0);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<FindTareasPorFiltro> rows = preparedQuery.execute(conn);
      return rows;
    }
  }

  @PostConstruct
  public void initializeContext() {
    this.context = new LiveSQLContext(this.liveSQLDialect, this.dataSource, new TypeSolver(null, this.liveSQLDialect), log);
    this.dyn = this.dynamicSQLBean.getDynamicSQL();
    this.initializeSelect0();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finer("SQL: " + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL: " + preparedQuery.getPreview());
    }
  }

}
