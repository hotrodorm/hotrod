// Autogenerated by HotRod -- Do not edit.

package app.persistence.dao;

import java.io.Serializable;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Types;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.dynamicsql.DynamicInsertQuery;
import org.hotrod.dynamicsql.DynamicModificationQuery;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.Parameters;
import org.hotrod.dynamicsql.PreparedModificationQuery;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.RowReader;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.dynamicsql.insert.PreparedInsertQuery;
import org.hotrod.dynamicsql.insert.PrimaryKeyRetrievalMode;
import org.hotrod.exceptions.PersistenceException;
import org.hotrod.interfaces.OrderBy;
import org.hotrod.livesql.LShield;
import org.hotrod.livesql.LiveSQL;
import org.hotrod.livesql.dialects.LiveSQLDialect;
import org.hotrod.livesql.metadata.AllColumns;
import org.hotrod.livesql.metadata.CharEntityColumn;
import org.hotrod.livesql.metadata.Name;
import org.hotrod.livesql.metadata.NumericEntityColumn;
import org.hotrod.livesql.metadata.View;
import org.hotrod.livesql.queries.DeleteWherePhase;
import org.hotrod.livesql.queries.LiveSQLContext;
import org.hotrod.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.livesql.queries.UpdateSetCompletePhase.Setter;
import org.hotrod.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.livesql.queries.typesolver.TypeHandler;
import org.hotrod.livesql.queries.typesolver.TypeSolver;
import org.hotrod.livesql.queries.typesolver.TypeSource;
import org.hotrod.livesql.util.CastUtil;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.utils.SQLUtil;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.stereotype.Component;

import app.persistence.layout.EmployeeBranchLayout;
import app.persistence.model.EmployeeBranch;

@Component
public class EmployeeBranchDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  private static final Logger log = Logger.getLogger(EmployeeBranchDAO.class.getName());

  @Autowired
  private DataSource dataSource;

  @Autowired
  private LiveSQL sql;

  private DynamicSQL dyn;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  private LiveSQLContext context;

  // ROW READER

  private final RowReader<EmployeeBranch> rowReader = new RowReader<EmployeeBranch>() {

    @Override
    public EmployeeBranch readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      EmployeeBranch row = applicationContext.getBean(EmployeeBranch.class);

      Integer col1 = rs.getInt("ID"); // ID
      if (rs.wasNull()) col1 = null;
      row.setId(col1);

      String col2 = rs.getString("NAME"); // NAME
      row.setName(col2);

      Integer col3 = rs.getInt("BRANCH_ID"); // BRANCH_ID
      if (rs.wasNull()) col3 = null;
      row.setBranchId(col3);

      Integer col4 = rs.getInt("VIP"); // VIP
      if (rs.wasNull()) col4 = null;
      row.setVip(col4);

      String col5 = rs.getString("REGION"); // REGION
      row.setRegion(col5);

      return row;
    }

  };

  // PARSE ROW

  public EmployeeBranch parseRow(Map<String, Object> row) {
    return parseRow(row, null, null);
  }

  public EmployeeBranch parseRow(Map<String, Object> row, String prefix) {
    return parseRow(row, prefix, null);
  }

  public EmployeeBranch parseRow(Map<String, Object> row, String prefix, String suffix) {
    EmployeeBranch m = applicationContext.getBean(EmployeeBranch.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    m.setId(CastUtil.toInteger((Number) row.get(p + "id" + s)));
    m.setName((String) row.get(p + "name" + s));
    m.setBranchId(CastUtil.toInteger((Number) row.get(p + "branchId" + s)));
    m.setVip(CastUtil.toInteger((Number) row.get(p + "vip" + s)));
    m.setRegion((String) row.get(p + "region" + s));
    return m;
  }

  // BASELINE

  public class EmployeeBranchBaseline {

    private Integer id;
    private String name;
    private Integer branchId;
    private Integer vip;
    private String region;

    public Integer getId() {
      return this.id;
    }

    public String getName() {
      return this.name;
    }

    public Integer getBranchId() {
      return this.branchId;
    }

    public Integer getVip() {
      return this.vip;
    }

    public String getRegion() {
      return this.region;
    }

  }

  public EmployeeBranchBaseline baseline(EmployeeBranch model) {
    EmployeeBranchBaseline b = new EmployeeBranchBaseline();
    b.id = model.getId();
    b.name = model.getName();
    b.branchId = model.getBranchId();
    b.vip = model.getVip();
    b.region = model.getRegion();
    return b;
  };

  // CLONE

  public EmployeeBranch clone(EmployeeBranchLayout layout) {
    EmployeeBranch m = this.applicationContext.getBean(EmployeeBranch.class);
    m.setId(layout.getId());
    m.setName(layout.getName());
    m.setBranchId(layout.getBranchId());
    m.setVip(layout.getVip());
    m.setRegion(layout.getRegion());
    return m;
  };

  // SELECT BY EXAMPLE

  private DynamicSelectQuery selectByExample;

  private void initializeSelectbyexample() {
    this.selectByExample = dyn
      .literaln("SELECT")
      .literaln("  id,")
      .literaln("  name,")
      .literaln("  branch_id,")
      .literaln("  vip,")
      .literaln("  region")
      .literaln("FROM employee_branch")
      .where("AND")
        .if_("f.id != null").literal("id = ").parameter("f.id").endif()
        .if_("f.name != null").literal("name = ").parameter("f.name").endif()
        .if_("f.branchId != null").literal("branch_id = ").parameter("f.branchId").endif()
        .if_("f.vip != null").literal("vip = ").parameter("f.vip").endif()
        .if_("f.region != null").literal("region = ").parameter("f.region").endif()
      .endwhere()
      .parameterInjection("ordering")
      .endSelectQuery();
  }

  public List<EmployeeBranch> select(EmployeeBranchLayout filter, EmployeeBranchOrderBy... orderBies) {
    Parameters params = this.dyn.newParameters();
    params.add("f", filter);
    String ordering = SQLUtil.render(orderBies);
    params.add("ordering", ordering);
    PreparedSelectQuery<EmployeeBranch> preparedQuery = this.selectByExample.prepare(params, this.rowReader);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<EmployeeBranch> rows = preparedQuery.execute(conn);
      return rows;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // SELECT BY CRITERIA

  public CriteriaWherePhase<EmployeeBranch> select(final EmployeeBranchView from, final Predicate predicate) {
    return new CriteriaWherePhase<EmployeeBranch>(this.context, from, predicate, this.rowReader);
  }

  // INSERT

  private DynamicInsertQuery insert;

  private void initializeInsert() {
    this.insert = dyn
      .literaln("INSERT INTO employee_branch (")
      .literaln("  id,")
      .literaln("  name,")
      .literaln("  branch_id,")
      .literaln("  vip,")
      .literaln("  region")
      .literaln(")")
      .literaln("VALUES(")
      .literal("  ").parameterNullable("l.id", Types.INTEGER).literaln(",")
      .literal("  ").parameterNullable("l.name", Types.VARCHAR).literaln(",")
      .literal("  ").parameterNullable("l.branchId", Types.INTEGER).literaln(",")
      .literal("  ").parameterNullable("l.vip", Types.INTEGER).literaln(",")
      .literal("  ").parameterNullable("l.region", Types.VARCHAR)
      .literal(")")
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public EmployeeBranch insert(EmployeeBranchLayout layout) {
    Parameters params = this.dyn.newParameters();
    params.add("l", layout);
    PreparedInsertQuery preparedQuery = this.insert.prepare(params);
    logQuery(preparedQuery);
    EmployeeBranch model = this.clone(layout);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
    return model;
  }

  // INSERT BY EXAMPLE

  private DynamicInsertQuery insertByExample;

  private void initializeInsertbyexample() {
    this.insertByExample = dyn
      .literaln("INSERT INTO employee_branch (")
      .if_("l.id != null").literal("id,\n").endif()
      .if_("l.name != null").literal("name,\n").endif()
      .if_("l.branchId != null").literal("branch_id,\n").endif()
      .if_("l.vip != null").literal("vip,\n").endif()
      .if_("l.region != null").literal("region\n").endif()
      .literaln(")")
      .literaln("VALUES(")
      .if_("l.id != null").parameter("l.id").literal(", ").endif()
      .if_("l.name != null").parameter("l.name").literal(", ").endif()
      .if_("l.branchId != null").parameter("l.branchId").literal(", ").endif()
      .if_("l.vip != null").parameter("l.vip").literal(", ").endif()
      .if_("l.region != null").parameter("l.region").endif()
      .literal(")")
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public EmployeeBranch insertByExample(EmployeeBranchLayout layout) {
    Parameters params = this.dyn.newParameters();
    params.add("l", layout);
    PreparedInsertQuery preparedQuery = this.insertByExample.prepare(params);
    logQuery(preparedQuery);
    EmployeeBranch model = this.clone(layout);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
    return model;
  }

  // UPDATE BY PRIMARY KEY -- Not available since the table does not have a primary key.

  // UPDATE BY EXAMPLE

  private DynamicModificationQuery updateByExample;

  private void initializeUpdatebyexample() {
    this.updateByExample = dyn
      .literal("UPDATE employee_branch")
      .set()
        .if_("v.id != null").literal("id = ").parameter("v.id").endif()
        .if_("v.name != null").literal("name = ").parameter("v.name").endif()
        .if_("v.branchId != null").literal("branch_id = ").parameter("v.branchId").endif()
        .if_("v.vip != null").literal("vip = ").parameter("v.vip").endif()
        .if_("v.region != null").literal("region = ").parameter("v.region").endif()
      .endset()
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.name != null").literal("name = ").parameter("e.name").endif()
        .if_("e.branchId != null").literal("branch_id = ").parameter("e.branchId").endif()
        .if_("e.vip != null").literal("vip = ").parameter("e.vip").endif()
        .if_("e.region != null").literal("region = ").parameter("e.region").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int update(EmployeeBranchLayout example, EmployeeBranchLayout values) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    params.add("v", values);
    PreparedModificationQuery preparedQuery = this.updateByExample.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // UPDATE BY CRITERIA

  public UpdateSetCompletePhase update(EmployeeBranchLayout values, EmployeeBranchView tableOrView,
      final Predicate predicate) {
    List<Setter> setters = new ArrayList<>();
    if (values.getId() != null) setters.add(new Setter(tableOrView.id, sql.val(values.getId())));
    if (values.getName() != null) setters.add(new Setter(tableOrView.name, sql.val(values.getName())));
    if (values.getBranchId() != null) setters.add(new Setter(tableOrView.branchId, sql.val(values.getBranchId())));
    if (values.getVip() != null) setters.add(new Setter(tableOrView.vip, sql.val(values.getVip())));
    if (values.getRegion() != null) setters.add(new Setter(tableOrView.region, sql.val(values.getRegion())));
    return new UpdateSetCompletePhase(this.context, tableOrView, setters, predicate);
  }

  // DELETE BY PRIMARY KEY -- Not available since the table does not have a primary key.

  // DELETE BY EXAMPLE

  private DynamicModificationQuery deleteByExample;

  private void initializeDeletebyexample() {
    this.deleteByExample = dyn
      .literaln("DELETE FROM employee_branch")
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.name != null").literal("name = ").parameter("e.name").endif()
        .if_("e.branchId != null").literal("branch_id = ").parameter("e.branchId").endif()
        .if_("e.vip != null").literal("vip = ").parameter("e.vip").endif()
        .if_("e.region != null").literal("region = ").parameter("e.region").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int delete(EmployeeBranchLayout example) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    PreparedModificationQuery preparedQuery = this.deleteByExample.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // DELETE BY CRITERIA

  public DeleteWherePhase delete(final EmployeeBranchView from, final Predicate predicate) {
    return new DeleteWherePhase(this.context, from, predicate);
  }

  // ORDER BY

  public enum EmployeeBranchOrderBy implements OrderBy {

    ID("id", true),
    ID$DESC("id", false),
    NAME("name", true),
    NAME$DESC("name", false),
    BRANCH_ID("branch_id", true),
    BRANCH_ID$DESC("branch_id", false),
    VIP("vip", true),
    VIP$DESC("vip", false),
    REGION("region", true),
    REGION$DESC("region", false);

    private String sqlColumnName;
    private boolean ascending;

    private EmployeeBranchOrderBy(String sqlColumnName, boolean ascending) {
      this.sqlColumnName = sqlColumnName;
      this.ascending = ascending;
    }

    @Override
    public String getSQLColumnName() {
      return this.sqlColumnName;
    }

    @Override
    public boolean isAscending() {
      return this.ascending;
    }

  }

  // VIEW METADATA

  public EmployeeBranchView newView() {
    return new EmployeeBranchView();
  }

  public EmployeeBranchView newView(final String alias) {
    return new EmployeeBranchView(alias);
  }

  public static class EmployeeBranchView extends View<EmployeeBranch> {

    public final NumericEntityColumn id = new NumericEntityColumn(this,
      "ID", "id", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.STATIC_DIALECT_RULE));
    public final CharEntityColumn name = new CharEntityColumn(this,
      "NAME", "name", "CHARACTER VARYING", 50, 0, TypeHandler.forClass(String.class, TypeSource.STATIC_DIALECT_RULE));
    public final NumericEntityColumn branchId = new NumericEntityColumn(this,
      "BRANCH_ID", "branchId", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.STATIC_DIALECT_RULE));
    public final NumericEntityColumn vip = new NumericEntityColumn(this,
      "VIP", "vip", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.STATIC_DIALECT_RULE));
    public final CharEntityColumn region = new CharEntityColumn(this,
      "REGION", "region", "CHARACTER VARYING", 10, 0, TypeHandler.forClass(String.class, TypeSource.STATIC_DIALECT_RULE));

    @Override
    public AllColumns star() {
      return new AllColumns(this.id, this.name, this.branchId, this.vip, this.region);
    }

    EmployeeBranchView() {
      super(null, null, Name.of("EMPLOYEE_BRANCH", false), "View", null, EmployeeBranchLayout.class, EmployeeBranch.class);
      initialize();
    }

    EmployeeBranchView(final String alias) {
      super(null, null, Name.of("EMPLOYEE_BRANCH", false), "View", alias, EmployeeBranchLayout.class, EmployeeBranch.class);
      initialize();
    }

    private void initialize() {
      super.columns = new ArrayList<>();
      super.columns.add(this.id);
      super.columns.add(this.name);
      super.columns.add(this.branchId);
      super.columns.add(this.vip);
      super.columns.add(this.region);
    }

  }

  // GETTERS

  public DataSource getDataSource() {
    return this.dataSource;
  }

  // INTERNAL METHODS

  @PostConstruct
  private void initializeContext() {
    LiveSQLDialect liveSQLDialect = LShield.getLiveSQLDialect(this.sql);
    this.context = new LiveSQLContext(liveSQLDialect, this.dataSource, new TypeSolver(null, liveSQLDialect), log);
    this.dyn = new DynamicSQL();
    this.initializeSelectbyexample();
    this.initializeInsert();
    this.initializeInsertbyexample();
    this.initializeUpdatebyexample();
    this.initializeDeletebyexample();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finest("SQL:\n" + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL:\n" + preparedQuery.getPreview());
    }
  }

}
