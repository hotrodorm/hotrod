// Autogenerated by HotRod -- Do not edit.

package app.persistence.dao.reporting;

import java.io.Serializable;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Types;
import java.time.LocalDate;
import java.util.ArrayList;
import java.util.List;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.data.RowReader;
import org.hotrod.dynamicsql.DynamicExpressionException;
import org.hotrod.dynamicsql.DynamicInsertQuery;
import org.hotrod.dynamicsql.DynamicModificationQuery;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.Parameters;
import org.hotrod.dynamicsql.PreparedModificationQuery;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.dynamicsql.insert.PreparedInsertQuery;
import org.hotrod.dynamicsql.insert.PrimaryKeyRetrievalMode;
import org.hotrod.interfaces.OrderBy;
import org.hotrod.runtime.livesql.LiveSQL;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.expressions.predicates.GeneralBooleanExpression;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.metadata.Name;
import org.hotrod.runtime.livesql.metadata.NumberEntityColumn;
import org.hotrod.runtime.livesql.metadata.ObjectEntityColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.LiveSQLContext;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase.Setter;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.typesolver.TypeHandler;
import org.hotrod.runtime.livesql.queries.typesolver.TypeSolver;
import org.hotrod.runtime.livesql.queries.typesolver.TypeSource;
import org.hotrod.runtime.livesql.util.DynamicSQLBean;
import org.hotrod.utils.SQLUtil;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.stereotype.Component;

import app.persistence.model.reporting.Payment;

@Component
public class PaymentDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  private static final Logger log = Logger.getLogger(PaymentDAO.class.getName());

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  @Autowired
  private DynamicSQLBean dynamicSQLBean;

  private DynamicSQL dyn;

  @Autowired
  private DataSource dataSource;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  @Autowired
  private LiveSQL sql;

  private LiveSQLContext context;

  // ROW READER

  private final RowReader<Payment> rowReader = new RowReader<Payment>() {

    @Override
    public Payment readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      Payment row = applicationContext.getBean(Payment.class);

      LocalDate col1 = rs.getObject(1, java.time.LocalDate.class); // PAYMENT_DATE
      row.setPaymentDate(col1);

      Integer col2 = rs.getInt(2); // INVOICE_ID
      if (rs.wasNull()) col2 = null;
      row.setInvoiceId(col2);

      Integer col3 = rs.getInt(3); // AMOUNT
      if (rs.wasNull()) col3 = null;
      row.setAmount(col3);

      return row;
    }

  };

  // BASELINE

  public class PaymentBaseline {

    private LocalDate paymentDate;
    private Integer invoiceId;
    private Integer amount;

    public LocalDate getPaymentDate() {
      return this.paymentDate;
    }

    public Integer getInvoiceId() {
      return this.invoiceId;
    }

    public Integer getAmount() {
      return this.amount;
    }

  }

  public PaymentBaseline baseline(Payment model) {
    PaymentBaseline b = new PaymentBaseline();
    b.paymentDate = model.getPaymentDate();
    b.invoiceId = model.getInvoiceId();
    b.amount = model.getAmount();
    return b;
  };

  // SELECT BY PRIMARY KEY -- Not available since the table does not have a primary key.

  // SELECT BY EXAMPLE

  private DynamicSelectQuery selectByExample;

  private void initializeSelectbyexample() {
    this.selectByExample = dyn
      .literaln("SELECT")
      .literaln("  payment_date,")
      .literaln("  invoice_id,")
      .literaln("  amount")
      .literaln("FROM payment")
      .where("AND")
        .if_("f.paymentDate != null").literal("payment_date = ").parameter("f.paymentDate").endif()
        .if_("f.invoiceId != null").literal("invoice_id = ").parameter("f.invoiceId").endif()
        .if_("f.amount != null").literal("amount = ").parameter("f.amount").endif()
      .endwhere()
      .parameterInjection("ordering")
      .endSelectQuery();
  }

  public List<Payment> select(Payment filter, PaymentOrderBy... orderBies) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("f", filter);
    String ordering = SQLUtil.render(orderBies);
    context.add("ordering", ordering);
    PreparedSelectQuery<Payment> preparedQuery = this.selectByExample.prepare(context, this.rowReader);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<Payment> rows = preparedQuery.execute(conn);
      return rows;
    }
  }

  // SELECT BY CRITERIA

  public CriteriaWherePhase<Payment> select(final PaymentTable from, final GeneralBooleanExpression predicate) {
    return new CriteriaWherePhase<Payment>(this.context, from, predicate, this.rowReader);
  }

  // INSERT

  private DynamicInsertQuery insert;

  private void initializeInsert() {
    this.insert = dyn
      .literaln("INSERT INTO payment (")
      .literaln("  payment_date,")
      .literaln("  invoice_id,")
      .literaln("  amount")
      .literaln(")")
      .literaln("VALUES(")
      .literal("  ").parameterNullable("m.paymentDate", Types.DATE).literaln(",")
      .literal("  ").parameterNullable("m.invoiceId", Types.INTEGER).literaln(",")
      .literal("  ").parameterNullable("m.amount", Types.INTEGER)
      .literal(")")
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public void insert(Payment model) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("m", model);
    PreparedInsertQuery preparedQuery = this.insert.prepare(context);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    }
  }

  // INSERT BY EXAMPLE

  private DynamicInsertQuery insertByExample;

  private void initializeInsertbyexample() {
    this.insertByExample = dyn
      .literaln("INSERT INTO payment (")
      .if_("m.paymentDate != null").literal("payment_date,\n").endif()
      .if_("m.invoiceId != null").literal("invoice_id,\n").endif()
      .if_("m.amount != null").literal("amount\n").endif()
      .literaln(")")
      .literaln("VALUES(")
      .if_("m.paymentDate != null").parameter("m.paymentDate").literal(", ").endif()
      .if_("m.invoiceId != null").parameter("m.invoiceId").literal(", ").endif()
      .if_("m.amount != null").parameter("m.amount").endif()
      .literal(")")
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public void insertByExample(Payment model) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("m", model);
    PreparedInsertQuery preparedQuery = this.insertByExample.prepare(context);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    }
  }

  // UPDATE BY PRIMARY KEY -- Not available since the table does not have a primary key.

  // UPDATE BY EXAMPLE

  private DynamicModificationQuery updateByExample;

  private void initializeUpdatebyexample() {
    this.updateByExample = dyn
      .literal("UPDATE payment")
      .set()
        .if_("v.paymentDate != null").literal("payment_date = ").parameter("v.paymentDate").endif()
        .if_("v.invoiceId != null").literal("invoice_id = ").parameter("v.invoiceId").endif()
        .if_("v.amount != null").literal("amount = ").parameter("v.amount").endif()
      .endset()
      .where("AND")
        .if_("e.paymentDate != null").literal("payment_date = ").parameter("e.paymentDate").endif()
        .if_("e.invoiceId != null").literal("invoice_id = ").parameter("e.invoiceId").endif()
        .if_("e.amount != null").literal("amount = ").parameter("e.amount").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int update(Payment example, Payment values) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("e", example);
    context.add("v", values);
    PreparedModificationQuery preparedQuery = this.updateByExample.prepare(context);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    }
  }

  // UPDATE BY CRITERIA

  public UpdateSetCompletePhase update(final Payment values, final PaymentTable tableOrView,
      final GeneralBooleanExpression predicate) {
    List<Setter> setters = new ArrayList<>();
    if (values.getPaymentDate() != null) setters.add(new Setter(tableOrView.paymentDate, sql.val(values.getPaymentDate())));
    if (values.getInvoiceId() != null) setters.add(new Setter(tableOrView.invoiceId, sql.val(values.getInvoiceId())));
    if (values.getAmount() != null) setters.add(new Setter(tableOrView.amount, sql.val(values.getAmount())));
    return new UpdateSetCompletePhase(this.context, tableOrView, setters, predicate);
  }

  // DELETE BY PRIMARY KEY -- Not available since the table does not have a primary key.

  // DELETE BY EXAMPLE

  private DynamicModificationQuery deleteByExample;

  private void initializeDeletebyexample() {
    this.deleteByExample = dyn
      .literal("DELETE FROM payment")
      .where("AND")
        .if_("e.paymentDate != null").literal("payment_date = ").parameter("e.paymentDate").endif()
        .if_("e.invoiceId != null").literal("invoice_id = ").parameter("e.invoiceId").endif()
        .if_("e.amount != null").literal("amount = ").parameter("e.amount").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int delete(Payment example) throws DynamicExpressionException, SQLException {
    Parameters context = this.dyn.newParameters();
    context.add("e", example);
    PreparedModificationQuery preparedQuery = this.deleteByExample.prepare(context);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    }
  }

  // DELETE BY CRITERIA

  public DeleteWherePhase delete(final PaymentTable from, final GeneralBooleanExpression predicate) {
    return new DeleteWherePhase(this.context, from, predicate);
  }

  // ORDER BY

  public enum PaymentOrderBy implements OrderBy {

    PAYMENT_DATE("payment_date", true),
    PAYMENT_DATE$DESC("payment_date", false),
    INVOICE_ID("invoice_id", true),
    INVOICE_ID$DESC("invoice_id", false),
    AMOUNT("amount", true),
    AMOUNT$DESC("amount", false);

    private String sqlColumnName;
    private boolean ascending;

    private PaymentOrderBy(String sqlColumnName, boolean ascending) {
      this.sqlColumnName = sqlColumnName;
      this.ascending = ascending;
    }

    public String getSQLColumnName() {
      return this.sqlColumnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database class org.hotrod.runtime.livesql.metadata.Table metadata

  public PaymentTable newTable() {
    return new PaymentTable();
  }

  public PaymentTable newTable(final String alias) {
    return new PaymentTable(alias);
  }

  public static class PaymentTable extends Table {

    // Properties

    public final ObjectEntityColumn paymentDate = new ObjectEntityColumn(this,
      "PAYMENT_DATE", "paymentDate", "DATE", 10, 0, TypeHandler.forClass(LocalDate.class, TypeSource.ENTITY_COLUMN));
    public final NumberEntityColumn invoiceId = new NumberEntityColumn(this,
      "INVOICE_ID", "invoiceId", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.ENTITY_COLUMN));
    public final NumberEntityColumn amount = new NumberEntityColumn(this,
      "AMOUNT", "amount", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.ENTITY_COLUMN));

    // Getters

    public AllColumns star() {
      return new AllColumns(this.paymentDate, this.invoiceId, this.amount);
    }

    // Constructors

    PaymentTable() {
      super(null, null, Name.of("PAYMENT", false), "Table", null);
      initialize();
    }

    PaymentTable(final String alias) {
      super(null, null, Name.of("PAYMENT", false), "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      super.columns = new ArrayList<>();
      super.columns.add(this.paymentDate);
      super.columns.add(this.invoiceId);
      super.columns.add(this.amount);
    }

  }

  @PostConstruct
  public void initializeContext() {
    this.context = new LiveSQLContext(this.liveSQLDialect, this.dataSource, new TypeSolver(null, this.liveSQLDialect), log);
    this.dyn = this.dynamicSQLBean.getDynamicSQL();
    this.initializeSelectbyexample();
    this.initializeInsert();
    this.initializeInsertbyexample();
    this.initializeUpdatebyexample();
    this.initializeDeletebyexample();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finer("SQL: " + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL: " + preparedQuery.getPreview());
    }
  }

}
