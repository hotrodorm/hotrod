// Generated by HotRod 5.1.19-SNAPSHOT on Apr 12, 2026 at 21:01:38

package app.persistence.dao;

import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Types;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.dynamicsql.DynamicInsertQuery;
import org.hotrod.dynamicsql.DynamicModificationQuery;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.Parameters;
import org.hotrod.dynamicsql.PreparedModificationQuery;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.RowReader;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.dynamicsql.insert.PreparedInsertQuery;
import org.hotrod.dynamicsql.insert.PrimaryKeyRetrievalMode;
import org.hotrod.exceptions.PersistenceException;
import org.hotrod.interfaces.OrderBy;
import org.hotrod.livesql.LShield;
import org.hotrod.livesql.LiveSQL;
import org.hotrod.livesql.LiveSQLLogging;
import org.hotrod.livesql.dialects.LiveSQLDialect;
import org.hotrod.livesql.metadata.AllColumns;
import org.hotrod.livesql.metadata.CharEntityColumn;
import org.hotrod.livesql.metadata.DirectEntityColumnMetaData;
import org.hotrod.livesql.metadata.Name;
import org.hotrod.livesql.metadata.NumericEntityColumn;
import org.hotrod.livesql.metadata.Table;
import org.hotrod.livesql.queries.DeleteWherePhase;
import org.hotrod.livesql.queries.LiveSQLContext;
import org.hotrod.livesql.queries.UpdateSetCompletePhase.Setter;
import org.hotrod.livesql.queries.UpdateWherePhase;
import org.hotrod.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.livesql.queries.typesolver.RuntimeTypeSolver;
import org.hotrod.livesql.queries.typesolver.TypeHandler;
import org.hotrod.livesql.queries.typesolver.TypeSource;
import org.hotrod.livesql.util.CastUtil;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.utils.SQLUtil;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.jdbc.datasource.DataSourceUtils;
import org.springframework.stereotype.Component;

import app.persistence.layout.OrderTypeALayout;
import app.persistence.model.OrderTypeA;

@Component
public class OrderTypeADAO implements ApplicationContextAware {

  private static final Logger log = Logger.getLogger(OrderTypeADAO.class.getName());

  private static final LiveSQLLogging livesql_log = LiveSQLLogging.of(
      () -> log.isLoggable(Level.FINE), msg -> log.fine(msg),
      () -> log.isLoggable(Level.FINER), msg -> log.finer(msg)
    );

  @Autowired
  private DataSource dataSource;

  @Autowired
  private LiveSQL sql;

  private DynamicSQL dyn;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  private LiveSQLContext context;

  // ROW READER

  private final RowReader<OrderTypeA> rowReader = new RowReader<OrderTypeA>() {

    @Override
    public OrderTypeA readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      OrderTypeA row = applicationContext.getBean(OrderTypeA.class);

      Integer col1 = rs.getInt("ID"); // ID
      if (rs.wasNull()) col1 = null;
      row.setId(col1);

      String col2 = rs.getString("case"); // case
      row.setOrderCase(col2);

      return row;
    }

  };

  // PARSE ROW

  public OrderTypeA parseRow(Map<String, Object> row) {
    return parseRow(row, null, null);
  }

  public OrderTypeA parseRow(Map<String, Object> row, String prefix) {
    return parseRow(row, prefix, null);
  }

  public OrderTypeA parseRow(Map<String, Object> row, String prefix, String suffix) {
    OrderTypeA m = applicationContext.getBean(OrderTypeA.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    m.setId(CastUtil.toInteger((Number) row.get(p + "id" + s)));
    m.setOrderCase((String) row.get(p + "orderCase" + s));
    return m;
  }

  // BASELINE

  public class OrderTypeABaseline {

    private Integer id;
    private String orderCase;

    public Integer getId() {
      return this.id;
    }

    public String getOrderCase() {
      return this.orderCase;
    }

  }

  public OrderTypeABaseline baseline(OrderTypeA model) {
    OrderTypeABaseline b = new OrderTypeABaseline();
    b.id = model.getId();
    b.orderCase = model.getOrderCase();
    return b;
  };

  // CLONE

  public OrderTypeA clone(OrderTypeALayout layout) {
    OrderTypeA m = this.applicationContext.getBean(OrderTypeA.class);
    m.setId(layout.getId());
    m.setOrderCase(layout.getOrderCase());
    return m;
  };

  // SELECT BY PRIMARY KEY

  private DynamicSelectQuery selectByPrimaryKey;

  private void initializeSelectbyprimarykey() {
    this.selectByPrimaryKey = dyn
      .literaln("SELECT")
      .literaln("  id,")
      .literaln("  \"case\"")
      .literaln("FROM \"Order Type α\"")
      .literal("WHERE id = ").parameter("f.id").literaln()
      .endSelectQuery();
  }

  public OrderTypeA select(Integer id) {
    if (id == null) return null;
    OrderTypeA filter = new OrderTypeA();
    filter.setId(id);
    Parameters params = this.dyn.newParameters();
    params.add("f", filter);
    PreparedSelectQuery<OrderTypeA> preparedQuery = this.selectByPrimaryKey.prepare(params, this.rowReader);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      List<OrderTypeA> rows = preparedQuery.execute(conn);
      if (rows.size() == 0) return null;
      if (rows.size() == 1) return rows.get(0);
      throw new PersistenceException("A single row at most was expected but received " + rows.size() + " rows.");
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // SELECT BY EXAMPLE

  private DynamicSelectQuery selectByExample;

  private void initializeSelectbyexample() {
    this.selectByExample = dyn
      .literaln("SELECT")
      .literaln("  id,")
      .literaln("  \"case\"")
      .literaln("FROM \"Order Type α\"")
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.orderCase != null").literal("\"case\" = ").parameter("e.orderCase").endif()
      .endwhere()
      .parameterInjection("ordering")
      .endSelectQuery();
  }

  public List<OrderTypeA> select(OrderTypeALayout example, OrderTypeAOrderBy... orderBies) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    String ordering = SQLUtil.render(orderBies);
    params.add("ordering", ordering);
    PreparedSelectQuery<OrderTypeA> preparedQuery = this.selectByExample.prepare(params, this.rowReader);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      List<OrderTypeA> rows = preparedQuery.execute(conn);
      return rows;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // SELECT BY CRITERIA

  public CriteriaWherePhase<OrderTypeA> select(final OrderTypeATable from, final Predicate predicate) {
    return new CriteriaWherePhase<OrderTypeA>(this.context, from, predicate, this.rowReader, livesql_log);
  }

  // INSERT

  private DynamicInsertQuery insert;

  private void initializeInsert() {
    this.insert = dyn
      .literal("INSERT INTO \"Order Type α\"")
      .trim(" (\n  ", ",\n  ", "\n) ")
        .literal("id")
        .literal("\"case\"")
      .endtrim()
      .literal("VALUES")
      .trim(" (\n  ", ",\n  ", "\n)")
        .parameterNullable("l.id", Types.INTEGER)
        .parameterNullable("l.orderCase", Types.VARCHAR)
      .endtrim()
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public OrderTypeA insert(OrderTypeALayout layout) {
    Parameters params = this.dyn.newParameters();
    params.add("l", layout);
    PreparedInsertQuery preparedQuery = this.insert.prepare(params);
    logQuery(preparedQuery);
    OrderTypeA model = this.clone(layout);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
    return model;
  }

  // INSERT BY EXAMPLE

  private DynamicInsertQuery insertByExample;

  private void initializeInsertbyexample() {
    this.insertByExample = dyn
      .literal("INSERT INTO \"Order Type α\"")
      .trim(" (\n  ", ",\n  ", "\n) ")
        .if_("e.id != null").literal("id").endif()
        .if_("e.orderCase != null").literal("\"case\"").endif()
      .endtrim()
      .literal("VALUES")
      .trim(" (\n  ", ",\n  ", "\n)")
        .if_("e.id != null").parameter("e.id").endif()
        .if_("e.orderCase != null").parameter("e.orderCase").endif()
      .endtrim()
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public OrderTypeA insertByExample(OrderTypeALayout example) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    PreparedInsertQuery preparedQuery = this.insertByExample.prepare(params);
    logQuery(preparedQuery);
    OrderTypeA model = this.clone(example);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
    return model;
  }

  // UPDATE BY PRIMARY KEY

  private DynamicModificationQuery updateByPK;

  private void initializeUpdatebypk() {
    this.updateByPK = dyn
      .literaln("UPDATE \"Order Type α\"")
      .literaln("SET")
      .literal("  id = ").parameterNullable("m.id", Types.INTEGER).literaln(",")
      .literal("  \"case\" = ").parameterNullable("m.orderCase", Types.VARCHAR).literaln()
      .literal("WHERE id = ").parameter("m.id").literaln()
    .endModificationQuery();
  }

  public int update(OrderTypeA model) {
    if (model.getId() == null) return 0;
    Parameters params = this.dyn.newParameters();
    params.add("m", model);
    PreparedModificationQuery preparedQuery = this.updateByPK.prepare(params);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // UPDATE BY EXAMPLE

  private DynamicModificationQuery updateByExample;

  private void initializeUpdatebyexample() {
    this.updateByExample = dyn
      .literal("UPDATE \"Order Type α\"")
      .set()
        .if_("v.id != null").literal("id = ").parameter("v.id").endif()
        .if_("v.orderCase != null").literal("\"case\" = ").parameter("v.orderCase").endif()
      .endset()
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.orderCase != null").literal("\"case\" = ").parameter("e.orderCase").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int update(OrderTypeALayout example, OrderTypeALayout values) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    params.add("v", values);
    PreparedModificationQuery preparedQuery = this.updateByExample.prepare(params);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // UPDATE BY CRITERIA

  public UpdateWherePhase update(OrderTypeALayout values, OrderTypeATable tableOrView,
      final Predicate predicate) {
    List<Setter> setters = new ArrayList<>();
    if (values.getId() != null) setters.add(new Setter(tableOrView.id, sql.val(values.getId())));
    if (values.getOrderCase() != null) setters.add(new Setter(tableOrView.orderCase, sql.val(values.getOrderCase())));
    return new UpdateWherePhase(this.context, tableOrView, setters, predicate, livesql_log);
  }

  // DELETE BY PRIMARY KEY

  private DynamicModificationQuery deleteByPK;

  private void initializeDeletebypk() {
    this.deleteByPK = dyn
      .literaln("DELETE FROM \"Order Type α\"")
      .literal("WHERE id = ").parameter("f.id").literaln()
      .endModificationQuery();
  }

  public int delete(Integer id) {
    if (id == null) return 0;
    OrderTypeA filter = new OrderTypeA();
    filter.setId(id);
    Parameters params = this.dyn.newParameters();
    params.add("f", filter);
    PreparedModificationQuery preparedQuery = this.deleteByPK.prepare(params);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // DELETE BY EXAMPLE

  private DynamicModificationQuery deleteByExample;

  private void initializeDeletebyexample() {
    this.deleteByExample = dyn
      .literaln("DELETE FROM \"Order Type α\"")
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.orderCase != null").literal("\"case\" = ").parameter("e.orderCase").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int delete(OrderTypeALayout example) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    PreparedModificationQuery preparedQuery = this.deleteByExample.prepare(params);
    logQuery(preparedQuery);
    Connection conn = null;
    try {
      conn = DataSourceUtils.getConnection(this.dataSource);
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    } finally {
      if (conn != null) {
        DataSourceUtils.releaseConnection(conn, this.dataSource);
      }
    }
  }

  // DELETE BY CRITERIA

  public DeleteWherePhase delete(final OrderTypeATable from, final Predicate predicate) {
    return new DeleteWherePhase(this.context, from, predicate, livesql_log);
  }

  // ORDER BY

  public enum OrderTypeAOrderBy implements OrderBy {

    ID("id", true),
    ID$DESC("id", false),
    ORDER_CASE("\"case\"", true),
    ORDER_CASE$DESC("\"case\"", false);

    private String sqlColumnName;
    private boolean ascending;

    private OrderTypeAOrderBy(String sqlColumnName, boolean ascending) {
      this.sqlColumnName = sqlColumnName;
      this.ascending = ascending;
    }

    @Override
    public String getSQLColumnName() {
      return this.sqlColumnName;
    }

    @Override
    public boolean isAscending() {
      return this.ascending;
    }

  }

  // TABLE METADATA

  static class MetaData {

    static final DirectEntityColumnMetaData ID_META_DATA = new DirectEntityColumnMetaData(
      Name.of("ID", false), "id", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.STATIC_DIALECT_RULE, "D9"));
    static final DirectEntityColumnMetaData ORDER_CASE_META_DATA = new DirectEntityColumnMetaData(
      Name.of("case", true), "orderCase", "CHARACTER VARYING", 200, 0, TypeHandler.forClass(String.class, TypeSource.STATIC_DIALECT_RULE, "D14"));

  }

  public OrderTypeATable newTable() {
    return new OrderTypeATable();
  }

  public OrderTypeATable newTable(final String alias) {
    return new OrderTypeATable(alias);
  }

  public static class OrderTypeATable extends Table<OrderTypeA> {

    public final NumericEntityColumn id = new NumericEntityColumn(this, MetaData.ID_META_DATA);
    public final CharEntityColumn orderCase = new CharEntityColumn(this, MetaData.ORDER_CASE_META_DATA);

    @Override
    public AllColumns star() {
      return new AllColumns(this.id, this.orderCase);
    }

    OrderTypeATable() {
      super(null, null, Name.of("Order Type α", true), "Table", null, OrderTypeALayout.class, OrderTypeA.class);
      initialize();
    }

    OrderTypeATable(final String alias) {
      super(null, null, Name.of("Order Type α", true), "Table", alias, OrderTypeALayout.class, OrderTypeA.class);
      initialize();
    }

    private void initialize() {
      super.columns = new ArrayList<>();
      super.columns.add(this.id);
      super.columns.add(this.orderCase);
    }

  }

  // GETTERS

  public DataSource getDataSource() {
    return this.dataSource;
  }

  // INTERNAL METHODS

  @PostConstruct
  private void initializeContext() {
    LiveSQLDialect liveSQLDialect = LShield.getLiveSQLDialect(this.sql);
    this.context = new LiveSQLContext(liveSQLDialect, this.dataSource, new RuntimeTypeSolver(null, liveSQLDialect));
    this.dyn = new DynamicSQL();
    this.initializeSelectbyprimarykey();
    this.initializeSelectbyexample();
    this.initializeInsert();
    this.initializeInsertbyexample();
    this.initializeUpdatebypk();
    this.initializeUpdatebyexample();
    this.initializeDeletebypk();
    this.initializeDeletebyexample();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finest("SQL:\n" + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL:\n" + preparedQuery.getPreview());
    }
  }

}
