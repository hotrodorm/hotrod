// Generated by HotRod 5.1.12-SNAPSHOT on Feb 20, 2026 at 20:32:56

package app.persistence.dao;

import java.io.Serializable;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Types;
import java.time.LocalDate;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.logging.Level;
import java.util.logging.Logger;

import javax.annotation.PostConstruct;
import javax.sql.DataSource;

import org.hotrod.dynamicsql.DynamicInsertQuery;
import org.hotrod.dynamicsql.DynamicModificationQuery;
import org.hotrod.dynamicsql.DynamicSelectQuery;
import org.hotrod.dynamicsql.Parameters;
import org.hotrod.dynamicsql.PreparedModificationQuery;
import org.hotrod.dynamicsql.PreparedQuery;
import org.hotrod.dynamicsql.PreparedSelectQuery;
import org.hotrod.dynamicsql.RowReader;
import org.hotrod.dynamicsql.assembler.DynamicSQL;
import org.hotrod.dynamicsql.insert.PreparedInsertQuery;
import org.hotrod.dynamicsql.insert.PrimaryKeyRetrievalMode;
import org.hotrod.exceptions.PersistenceException;
import org.hotrod.interfaces.OrderBy;
import org.hotrod.livesql.LShield;
import org.hotrod.livesql.LiveSQL;
import org.hotrod.livesql.LiveSQLLogging;
import org.hotrod.livesql.dialects.LiveSQLDialect;
import org.hotrod.livesql.metadata.AllColumns;
import org.hotrod.livesql.metadata.CharEntityColumn;
import org.hotrod.livesql.metadata.CharEntityColumnMetaData;
import org.hotrod.livesql.metadata.DateTimeEntityColumn;
import org.hotrod.livesql.metadata.DateTimeEntityColumnMetaData;
import org.hotrod.livesql.metadata.Name;
import org.hotrod.livesql.metadata.NumericEntityColumn;
import org.hotrod.livesql.metadata.NumericEntityColumnMetaData;
import org.hotrod.livesql.metadata.Table;
import org.hotrod.livesql.queries.DeleteWherePhase;
import org.hotrod.livesql.queries.LiveSQLContext;
import org.hotrod.livesql.queries.UpdateSetCompletePhase.Setter;
import org.hotrod.livesql.queries.UpdateWherePhase;
import org.hotrod.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.livesql.queries.typesolver.RuntimeTypeSolver;
import org.hotrod.livesql.queries.typesolver.TypeHandler;
import org.hotrod.livesql.queries.typesolver.TypeSource;
import org.hotrod.livesql.util.CastUtil;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.utils.SQLUtil;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;
import org.springframework.stereotype.Component;

import app.persistence.layout.BranchLayout;
import app.persistence.model.Branch;

@Component
public class BranchDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  private static final Logger log = Logger.getLogger(BranchDAO.class.getName());

  private static final LiveSQLLogging livesql_log = LiveSQLLogging.of(
      () -> log.isLoggable(Level.FINE), msg -> log.fine(msg),
      () -> log.isLoggable(Level.FINER), msg -> log.finer(msg)
    );

  @Autowired
  private DataSource dataSource;

  @Autowired
  private LiveSQL sql;

  private DynamicSQL dyn;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  private LiveSQLContext context;

  // ROW READER

  private final RowReader<Branch> rowReader = new RowReader<Branch>() {

    @Override
    public Branch readRowFrom(ResultSet rs, Connection conn) throws SQLException {
      Branch row = applicationContext.getBean(Branch.class);

      Integer col1 = rs.getInt("ID"); // ID
      if (rs.wasNull()) col1 = null;
      row.setId(col1);

      String col2 = rs.getString("NAME"); // NAME
      row.setName(col2);

      LocalDate col3 = rs.getObject("ESTABLISHED", LocalDate.class); // ESTABLISHED
      row.setEstablished(col3);

      return row;
    }

  };

  // PARSE ROW

  public Branch parseRow(Map<String, Object> row) {
    return parseRow(row, null, null);
  }

  public Branch parseRow(Map<String, Object> row, String prefix) {
    return parseRow(row, prefix, null);
  }

  public Branch parseRow(Map<String, Object> row, String prefix, String suffix) {
    Branch m = applicationContext.getBean(Branch.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    m.setId(CastUtil.toInteger((Number) row.get(p + "id" + s)));
    m.setName((String) row.get(p + "name" + s));
    m.setEstablished((LocalDate) row.get(p + "established" + s));
    return m;
  }

  // BASELINE

  public class BranchBaseline {

    private Integer id;
    private String name;
    private LocalDate established;

    public Integer getId() {
      return this.id;
    }

    public String getName() {
      return this.name;
    }

    public LocalDate getEstablished() {
      return this.established;
    }

  }

  public BranchBaseline baseline(Branch model) {
    BranchBaseline b = new BranchBaseline();
    b.id = model.getId();
    b.name = model.getName();
    b.established = model.getEstablished();
    return b;
  };

  // CLONE

  public Branch clone(BranchLayout layout) {
    Branch m = this.applicationContext.getBean(Branch.class);
    m.setId(layout.getId());
    m.setName(layout.getName());
    m.setEstablished(layout.getEstablished());
    return m;
  };

  // SELECT BY PRIMARY KEY

  private DynamicSelectQuery selectByPrimaryKey;

  private void initializeSelectbyprimarykey() {
    this.selectByPrimaryKey = dyn
      .literaln("SELECT")
      .literaln("  id,")
      .literaln("  name,")
      .literaln("  established")
      .literaln("FROM branch")
      .literal("WHERE id = ").parameter("f.id").literaln()
      .endSelectQuery();
  }

  public Branch select(Integer id) {
    if (id == null) return null;
    Branch filter = new Branch();
    filter.setId(id);
    Parameters params = this.dyn.newParameters();
    params.add("f", filter);
    PreparedSelectQuery<Branch> preparedQuery = this.selectByPrimaryKey.prepare(params, this.rowReader);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<Branch> rows = preparedQuery.execute(conn);
      if (rows.size() == 0) return null;
      if (rows.size() == 1) return rows.get(0);
      throw new PersistenceException("A single row at most was expected but received " + rows.size() + " rows.");
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // SELECT BY EXAMPLE

  private DynamicSelectQuery selectByExample;

  private void initializeSelectbyexample() {
    this.selectByExample = dyn
      .literaln("SELECT")
      .literaln("  id,")
      .literaln("  name,")
      .literaln("  established")
      .literaln("FROM branch")
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.name != null").literal("name = ").parameter("e.name").endif()
        .if_("e.established != null").literal("established = ").parameter("e.established").endif()
      .endwhere()
      .parameterInjection("ordering")
      .endSelectQuery();
  }

  public List<Branch> select(BranchLayout example, BranchOrderBy... orderBies) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    String ordering = SQLUtil.render(orderBies);
    params.add("ordering", ordering);
    PreparedSelectQuery<Branch> preparedQuery = this.selectByExample.prepare(params, this.rowReader);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      List<Branch> rows = preparedQuery.execute(conn);
      return rows;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // SELECT BY CRITERIA

  public CriteriaWherePhase<Branch> select(final BranchTable from, final Predicate predicate) {
    return new CriteriaWherePhase<Branch>(this.context, from, predicate, this.rowReader, livesql_log);
  }

  // INSERT

  private DynamicInsertQuery insert;

  private void initializeInsert() {
    this.insert = dyn
      .literal("INSERT INTO branch")
      .trim(" (\n  ", ",\n  ", "\n) ")
        .literal("id")
        .literal("name")
        .literal("established")
      .endtrim()
      .literal("VALUES")
      .trim(" (\n  ", ",\n  ", "\n)")
        .parameterNullable("l.id", Types.INTEGER)
        .parameterNullable("l.name", Types.VARCHAR)
        .parameterNullable("l.established", Types.DATE)
      .endtrim()
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public Branch insert(BranchLayout layout) {
    Parameters params = this.dyn.newParameters();
    params.add("l", layout);
    PreparedInsertQuery preparedQuery = this.insert.prepare(params);
    logQuery(preparedQuery);
    Branch model = this.clone(layout);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
    return model;
  }

  // INSERT BY EXAMPLE

  private DynamicInsertQuery insertByExample;

  private void initializeInsertbyexample() {
    this.insertByExample = dyn
      .literal("INSERT INTO branch")
      .trim(" (\n  ", ",\n  ", "\n) ")
        .if_("e.id != null").literal("id").endif()
        .if_("e.name != null").literal("name").endif()
        .if_("e.established != null").literal("established").endif()
      .endtrim()
      .literal("VALUES")
      .trim(" (\n  ", ",\n  ", "\n)")
        .if_("e.id != null").parameter("e.id").endif()
        .if_("e.name != null").parameter("e.name").endif()
        .if_("e.established != null").parameter("e.established").endif()
      .endtrim()
      .endInsertQuery(PrimaryKeyRetrievalMode.NO_RETRIEVAL);
  }

  public Branch insertByExample(BranchLayout example) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    PreparedInsertQuery preparedQuery = this.insertByExample.prepare(params);
    logQuery(preparedQuery);
    Branch model = this.clone(example);
    try (Connection conn = this.dataSource.getConnection()) {
      preparedQuery.execute(conn);
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
    return model;
  }

  // UPDATE BY PRIMARY KEY

  private DynamicModificationQuery updateByPK;

  private void initializeUpdatebypk() {
    this.updateByPK = dyn
      .literaln("UPDATE branch")
      .literaln("SET")
      .literal("  id = ").parameterNullable("m.id", Types.INTEGER).literaln(",")
      .literal("  name = ").parameterNullable("m.name", Types.VARCHAR).literaln(",")
      .literal("  established = ").parameterNullable("m.established", Types.DATE).literaln()
      .literal("WHERE id = ").parameter("m.id").literaln()
    .endModificationQuery();
  }

  public int update(Branch model) {
    if (model.getId() == null) return 0;
    Parameters params = this.dyn.newParameters();
    params.add("m", model);
    PreparedModificationQuery preparedQuery = this.updateByPK.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // UPDATE BY EXAMPLE

  private DynamicModificationQuery updateByExample;

  private void initializeUpdatebyexample() {
    this.updateByExample = dyn
      .literal("UPDATE branch")
      .set()
        .if_("v.id != null").literal("id = ").parameter("v.id").endif()
        .if_("v.name != null").literal("name = ").parameter("v.name").endif()
        .if_("v.established != null").literal("established = ").parameter("v.established").endif()
      .endset()
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.name != null").literal("name = ").parameter("e.name").endif()
        .if_("e.established != null").literal("established = ").parameter("e.established").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int update(BranchLayout example, BranchLayout values) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    params.add("v", values);
    PreparedModificationQuery preparedQuery = this.updateByExample.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // UPDATE BY CRITERIA

  public UpdateWherePhase update(BranchLayout values, BranchTable tableOrView,
      final Predicate predicate) {
    List<Setter> setters = new ArrayList<>();
    if (values.getId() != null) setters.add(new Setter(tableOrView.id, sql.val(values.getId())));
    if (values.getName() != null) setters.add(new Setter(tableOrView.name, sql.val(values.getName())));
    if (values.getEstablished() != null) setters.add(new Setter(tableOrView.established, sql.val(values.getEstablished())));
    return new UpdateWherePhase(this.context, tableOrView, setters, predicate, livesql_log);
  }

  // DELETE BY PRIMARY KEY

  private DynamicModificationQuery deleteByPK;

  private void initializeDeletebypk() {
    this.deleteByPK = dyn
      .literaln("DELETE FROM branch")
      .literal("WHERE id = ").parameter("f.id").literaln()
      .endModificationQuery();
  }

  public int delete(Integer id) {
    if (id == null) return 0;
    Branch filter = new Branch();
    filter.setId(id);
    Parameters params = this.dyn.newParameters();
    params.add("f", filter);
    PreparedModificationQuery preparedQuery = this.deleteByPK.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // DELETE BY EXAMPLE

  private DynamicModificationQuery deleteByExample;

  private void initializeDeletebyexample() {
    this.deleteByExample = dyn
      .literaln("DELETE FROM branch")
      .where("AND")
        .if_("e.id != null").literal("id = ").parameter("e.id").endif()
        .if_("e.name != null").literal("name = ").parameter("e.name").endif()
        .if_("e.established != null").literal("established = ").parameter("e.established").endif()
      .endwhere()
      .endModificationQuery();
  }

  public int delete(BranchLayout example) {
    Parameters params = this.dyn.newParameters();
    params.add("e", example);
    PreparedModificationQuery preparedQuery = this.deleteByExample.prepare(params);
    logQuery(preparedQuery);
    try (Connection conn = this.dataSource.getConnection()) {
      int count = preparedQuery.execute(conn);
      return count;
    } catch (SQLException e) {
      throw new PersistenceException(e);
    }
  }

  // DELETE BY CRITERIA

  public DeleteWherePhase delete(final BranchTable from, final Predicate predicate) {
    return new DeleteWherePhase(this.context, from, predicate, livesql_log);
  }

  // ORDER BY

  public enum BranchOrderBy implements OrderBy {

    ID("id", true),
    ID$DESC("id", false),
    NAME("name", true),
    NAME$DESC("name", false),
    ESTABLISHED("established", true),
    ESTABLISHED$DESC("established", false);

    private String sqlColumnName;
    private boolean ascending;

    private BranchOrderBy(String sqlColumnName, boolean ascending) {
      this.sqlColumnName = sqlColumnName;
      this.ascending = ascending;
    }

    @Override
    public String getSQLColumnName() {
      return this.sqlColumnName;
    }

    @Override
    public boolean isAscending() {
      return this.ascending;
    }

  }

  // TABLE METADATA

  public BranchTable newTable() {
    return new BranchTable();
  }

  public BranchTable newTable(final String alias) {
    return new BranchTable(alias);
  }

  public static class BranchTable extends Table<Branch> {

    private static final NumericEntityColumnMetaData _ID = new NumericEntityColumnMetaData(
      "ID", "id", "INTEGER", 32, 0, TypeHandler.forClass(Integer.class, TypeSource.STATIC_DIALECT_RULE, "D9"));
    private static final CharEntityColumnMetaData _NAME = new CharEntityColumnMetaData(
      "NAME", "name", "CHARACTER VARYING", 20, 0, TypeHandler.forClass(String.class, TypeSource.STATIC_DIALECT_RULE, "D14"));
    private static final DateTimeEntityColumnMetaData _ESTABLISHED = new DateTimeEntityColumnMetaData(
      "ESTABLISHED", "established", "DATE", 10, 0, TypeHandler.forClass(LocalDate.class, TypeSource.STATIC_DIALECT_RULE, "D16"));

    public final NumericEntityColumn id = new NumericEntityColumn(this, _ID);
    public final CharEntityColumn name = new CharEntityColumn(this, _NAME);
    public final DateTimeEntityColumn established = new DateTimeEntityColumn(this, _ESTABLISHED);

    @Override
    public AllColumns star() {
      return new AllColumns(this.id, this.name, this.established);
    }

    BranchTable() {
      super(null, null, Name.of("BRANCH", false), "Table", null, BranchLayout.class, Branch.class);
      initialize();
    }

    BranchTable(final String alias) {
      super(null, null, Name.of("BRANCH", false), "Table", alias, BranchLayout.class, Branch.class);
      initialize();
    }

    private void initialize() {
      super.columns = new ArrayList<>();
      super.columns.add(this.id);
      super.columns.add(this.name);
      super.columns.add(this.established);
    }

  }

  // GETTERS

  public DataSource getDataSource() {
    return this.dataSource;
  }

  // INTERNAL METHODS

  @PostConstruct
  private void initializeContext() {
    LiveSQLDialect liveSQLDialect = LShield.getLiveSQLDialect(this.sql);
    this.context = new LiveSQLContext(liveSQLDialect, this.dataSource, new RuntimeTypeSolver(null, liveSQLDialect));
    this.dyn = new DynamicSQL();
    this.initializeSelectbyprimarykey();
    this.initializeSelectbyexample();
    this.initializeInsert();
    this.initializeInsertbyexample();
    this.initializeUpdatebypk();
    this.initializeUpdatebyexample();
    this.initializeDeletebypk();
    this.initializeDeletebyexample();
  }

  private void logQuery(PreparedQuery preparedQuery) {
    if (log.isLoggable(Level.FINER)) {
      log.finest("SQL:\n" + preparedQuery.getPreview(true));
    } else if (log.isLoggable(Level.FINE)) {
      log.fine("SQL:\n" + preparedQuery.getPreview());
    }
  }

}
