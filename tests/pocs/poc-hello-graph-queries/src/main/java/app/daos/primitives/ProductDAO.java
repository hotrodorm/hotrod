// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.Product;
import app.daos.ProductVO;

import java.lang.Override;
import java.util.Map;
import java.util.ArrayList;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.spring.SpringBeanObjectFactory;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.LiveSQLMapper;
import org.hotrod.runtime.livesql.util.CastUtil;
import javax.annotation.PostConstruct;
import org.hotrod.runtime.livesql.metadata.Column;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.hotrod.runtime.livesql.queries.LiveSQLContext;
import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class ProductDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  @Autowired
  private LiveSQLMapper liveSQLMapper;

  @Autowired
  private SpringBeanObjectFactory springBeanObjectFactory;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
    this.sqlSession.getConfiguration().setObjectFactory(this.springBeanObjectFactory);
  }

  private LiveSQLContext context;

  @PostConstruct
  public void initializeContext() {
    this.context = new LiveSQLContext(this.liveSQLDialect, this.sqlSession, this.liveSQLMapper);
  }

  // Row Parser

  public app.daos.ProductVO parseRow(Map<String, Object> m) {
    return parseRow(m, null, null);
  }

  public app.daos.ProductVO parseRow(Map<String, Object> m, String prefix) {
    return parseRow(m, prefix, null);
  }

  public app.daos.ProductVO parseRow(Map<String, Object> m, String prefix, String suffix) {
    app.daos.ProductVO mo = this.applicationContext.getBean(app.daos.ProductVO.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    mo.setId(CastUtil.toInteger((Number) m.get(p + "id" + s)));
    mo.setName((java.lang.String) m.get(p + "name" + s));
    mo.setCategoryId(CastUtil.toInteger((Number) m.get(p + "categoryId" + s)));
    return mo;
  }

  // select by primary key

  public app.daos.ProductVO select(final java.lang.Integer id) {
    if (id == null)
      return null;
    app.daos.ProductVO vo = new app.daos.ProductVO();
    vo.setId(id);
    return this.sqlSession.selectOne("mappers.product.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.ProductVO> select(final app.daos.primitives.Product example, final ProductOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.Product, ProductOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.product.selectByExample", dwo);
  }

  public Cursor<app.daos.ProductVO> selectCursor(final app.daos.primitives.Product example, final ProductOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.Product, ProductOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.ProductVO>(this.sqlSession.selectCursor("mappers.product.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.ProductVO> select(final ProductDAO.ProductTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.ProductVO>(this.context, "mappers.product.selectByCriteria",
        from, predicate);
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.ProductVO insert(final app.daos.primitives.Product vo) {
    String id = "mappers.product.insert";
    this.sqlSession.insert(id, vo);
    app.daos.ProductVO mo = springBeanObjectFactory.create(app.daos.ProductVO.class);
    mo.setId(vo.getId());
    mo.setName(vo.getName());
    mo.setCategoryId(vo.getCategoryId());
    return mo;
  }

  // update by PK

  public int update(final app.daos.ProductVO vo) {
    if (vo.getId() == null) return 0;
    return this.sqlSession.update("mappers.product.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Integer id) {
    if (id == null) return 0;
    app.daos.ProductVO vo = new app.daos.ProductVO();
    vo.setId(id);
    if (vo.getId() == null) return 0;
    return this.sqlSession.delete("mappers.product.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.Product example, final app.daos.primitives.Product updateValues) {
    UpdateByExampleDao<app.daos.primitives.Product> fvd = //
      new UpdateByExampleDao<app.daos.primitives.Product>(example, updateValues);
    return this.sqlSession.update("mappers.product.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.Product updateValues, final ProductDAO.ProductTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getName() != null) values.put("name", updateValues.getName());
    if (updateValues.getCategoryId() != null) values.put("category_id", updateValues.getCategoryId());
    return new UpdateSetCompletePhase(this.context, "mappers.product.updateByCriteria", tableOrView,  predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.Product example) {
    return this.sqlSession.delete("mappers.product.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final ProductDAO.ProductTable from, final Predicate predicate) {
    return new DeleteWherePhase(this.context, "mappers.product.deleteByCriteria", from, predicate);
  }

  // DAO ordering

  public enum ProductOrderBy implements OrderBy {

    ID("product", "id", true), //
    ID$DESC("product", "id", false), //
    NAME("product", "name", true), //
    NAME$DESC("product", "name", false), //
    NAME$CASEINSENSITIVE("product", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("product", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("product", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("product", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("product", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("product", "lower(name), name", true), //
    CATEGORY_ID("product", "category_id", true), //
    CATEGORY_ID$DESC("product", "category_id", false);

    private ProductOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static ProductTable newTable() {
    return new ProductTable();
  }

  public static ProductTable newTable(final String alias) {
    return new ProductTable(alias);
  }

  public static class ProductTable extends Table {

    // Properties

    public NumberColumn id;
    public StringColumn name;
    public NumberColumn categoryId;

    // Getters

    public AllColumns star() {
      return new AllColumns(this.id, this.name, this.categoryId);
    }

    // Constructors

    ProductTable() {
      super(null, null, "PRODUCT", "Table", null);
      initialize();
    }

    ProductTable(final String alias) {
      super(null, null, "PRODUCT", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      super.columns = new ArrayList<>();
      this.id = new NumberColumn(this, "ID", "id", "INTEGER", 32, 0);
      super.columns.add(this.id);
      this.name = new StringColumn(this, "NAME", "name", "CHARACTER VARYING", 20, 0);
      super.columns.add(this.name);
      this.categoryId = new NumberColumn(this, "CATEGORY_ID", "categoryId", "INTEGER", 32, 0);
      super.columns.add(this.categoryId);
    }

  }

}
