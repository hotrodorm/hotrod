// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractHouseVO;
import app.daos.HouseVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class HouseDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.HouseVO> select(final app.daos.primitives.AbstractHouseVO example, final HouseOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractHouseVO, HouseOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.house.selectByExample", dwo);
  }

  public Cursor<app.daos.HouseVO> selectCursor(final app.daos.primitives.AbstractHouseVO example, final HouseOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractHouseVO, HouseOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.HouseVO>(this.sqlSession.selectCursor("mappers.house.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.HouseVO> select(final HouseDAO.HouseTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.HouseVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.house.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.HouseVO insert(final app.daos.primitives.AbstractHouseVO vo) {
    String id = "mappers.house.insert";
    this.sqlSession.insert(id, vo);
    app.daos.HouseVO mo = new app.daos.HouseVO();
    mo.setAddress(vo.getAddress());
    mo.setPrice(vo.getPrice());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.primitives.AbstractHouseVO example, final app.daos.primitives.AbstractHouseVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractHouseVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractHouseVO>(example, updateValues);
    return this.sqlSession.update("mappers.house.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractHouseVO updateValues, final HouseDAO.HouseTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getAddress() != null) values.put("address", updateValues.getAddress());
    if (updateValues.getPrice() != null) values.put("price", updateValues.getPrice());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.house.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractHouseVO example) {
    return this.sqlSession.delete("mappers.house.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final HouseDAO.HouseTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.house.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum HouseOrderBy implements OrderBy {

    ADDRESS("public.house", "address", true), //
    ADDRESS$DESC("public.house", "address", false), //
    ADDRESS$CASEINSENSITIVE("public.house", "lower(address)", true), //
    ADDRESS$CASEINSENSITIVE_STABLE_FORWARD("public.house", "lower(address), address", true), //
    ADDRESS$CASEINSENSITIVE_STABLE_REVERSE("public.house", "lower(address), address", false), //
    ADDRESS$DESC_CASEINSENSITIVE("public.house", "lower(address)", false), //
    ADDRESS$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.house", "lower(address), address", false), //
    ADDRESS$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.house", "lower(address), address", true), //
    PRICE("public.house", "price", true), //
    PRICE$DESC("public.house", "price", false);

    private HouseOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static HouseTable newTable() {
    return new HouseTable();
  }

  public static HouseTable newTable(final String alias) {
    return new HouseTable(alias);
  }

  public static class HouseTable extends Table {

    // Properties

    public StringColumn address;
    public NumberColumn price;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.address, this.price);
    }

    // Constructors

    HouseTable() {
      super(null, "public", "house", "Table", null);
      initialize();
    }

    HouseTable(final String alias) {
      super(null, "public", "house", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.address = new StringColumn(this, "address", "address", "varchar", 50, 0);
      this.price = new NumberColumn(this, "price", "price", "int4", 10, 0);
    }

  }

}
