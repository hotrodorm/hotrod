// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractAccountVO;
import app.daos.AccountVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class AccountDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.AccountVO select(final java.lang.Integer id) {
    if (id == null)
      return null;
    app.daos.AccountVO vo = new app.daos.AccountVO();
    vo.setId(id);
    return this.sqlSession.selectOne("mappers.account.selectByPK", vo);
  }

  // select by unique indexes

  public app.daos.AccountVO selectByUIName(final java.lang.String name) {
    if (name == null)
      return null;
    app.daos.AccountVO vo = new app.daos.AccountVO();
    vo.setName(name);
    return this.sqlSession.selectOne("mappers.account.selectByUIName", vo);
  }

  // select by example

  public List<app.daos.AccountVO> select(final app.daos.primitives.AbstractAccountVO example, final AccountOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractAccountVO, AccountOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.account.selectByExample", dwo);
  }

  public Cursor<app.daos.AccountVO> selectCursor(final app.daos.primitives.AbstractAccountVO example, final AccountOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractAccountVO, AccountOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.AccountVO>(this.sqlSession.selectCursor("mappers.account.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.AccountVO> select(final AccountDAO.AccountTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.AccountVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.account.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.AccountVO insert(final app.daos.primitives.AbstractAccountVO vo) {
    return insert(vo, false);
  }

  public app.daos.AccountVO insert(final app.daos.primitives.AbstractAccountVO vo, final boolean retrieveDefaults) {
    String id = retrieveDefaults ? "mappers.account.insertRetrievingDefaults" : "mappers.account.insert";
    int rows = this.sqlSession.insert(id, vo);
    app.daos.AccountVO mo = new app.daos.AccountVO();
    mo.setId(vo.getId());
    mo.setName(vo.getName());
    mo.setType(vo.getType());
    mo.setCurrentBalance(vo.getCurrentBalance());
    mo.setCreatedOn(vo.getCreatedOn());
    mo.setActive(vo.getActive());
    return mo;
  }

  // update by PK

  public int update(final app.daos.AccountVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.update("mappers.account.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Integer id) {
    if (id == null) return 0;
    app.daos.AccountVO vo = new app.daos.AccountVO();
    vo.setId(id);
    if (vo.id == null) return 0;
    return this.sqlSession.delete("mappers.account.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.AbstractAccountVO example, final app.daos.primitives.AbstractAccountVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractAccountVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractAccountVO>(example, updateValues);
    return this.sqlSession.update("mappers.account.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractAccountVO updateValues, final AccountDAO.AccountTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getName() != null) values.put("name", updateValues.getName());
    if (updateValues.getType() != null) values.put("type", updateValues.getType());
    if (updateValues.getCurrentBalance() != null) values.put("current_balance", updateValues.getCurrentBalance());
    if (updateValues.getCreatedOn() != null) values.put("created_on", updateValues.getCreatedOn());
    if (updateValues.getActive() != null) values.put("active", updateValues.getActive());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.account.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractAccountVO example) {
    return this.sqlSession.delete("mappers.account.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final AccountDAO.AccountTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.account.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum AccountOrderBy implements OrderBy {

    ID("public.account", "id", true), //
    ID$DESC("public.account", "id", false), //
    NAME("public.account", "name", true), //
    NAME$DESC("public.account", "name", false), //
    NAME$CASEINSENSITIVE("public.account", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("public.account", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("public.account", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("public.account", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.account", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.account", "lower(name), name", true), //
    TYPE("public.account", "type", true), //
    TYPE$DESC("public.account", "type", false), //
    TYPE$CASEINSENSITIVE("public.account", "lower(type)", true), //
    TYPE$CASEINSENSITIVE_STABLE_FORWARD("public.account", "lower(type), type", true), //
    TYPE$CASEINSENSITIVE_STABLE_REVERSE("public.account", "lower(type), type", false), //
    TYPE$DESC_CASEINSENSITIVE("public.account", "lower(type)", false), //
    TYPE$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.account", "lower(type), type", false), //
    TYPE$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.account", "lower(type), type", true), //
    CURRENT_BALANCE("public.account", "current_balance", true), //
    CURRENT_BALANCE$DESC("public.account", "current_balance", false), //
    CREATED_ON("public.account", "created_on", true), //
    CREATED_ON$DESC("public.account", "created_on", false), //
    ACTIVE("public.account", "active", true), //
    ACTIVE$DESC("public.account", "active", false);

    private AccountOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static AccountTable newTable() {
    return new AccountTable();
  }

  public static AccountTable newTable(final String alias) {
    return new AccountTable(alias);
  }

  public static class AccountTable extends Table {

    // Properties

    public NumberColumn id;
    public StringColumn name;
    public StringColumn type;
    public NumberColumn currentBalance;
    public DateTimeColumn createdOn;
    public NumberColumn active;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.name, this.type, this.currentBalance, this.createdOn, this.active);
    }

    // Constructors

    AccountTable() {
      super(null, "public", "account", "Table", null);
      initialize();
    }

    AccountTable(final String alias) {
      super(null, "public", "account", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "serial", 10, 0);
      this.name = new StringColumn(this, "name", "name", "varchar", 100, 0);
      this.type = new StringColumn(this, "type", "type", "varchar", 10, 0);
      this.currentBalance = new NumberColumn(this, "current_balance", "currentBalance", "int4", 10, 0);
      this.createdOn = new DateTimeColumn(this, "created_on", "createdOn", "timestamp", 29, 6);
      this.active = new NumberColumn(this, "active", "active", "int4", 10, 0);
    }

  }

}
