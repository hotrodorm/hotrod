// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractIslandVO;
import app.daos.IslandVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class IslandDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.IslandVO> select(final app.daos.primitives.AbstractIslandVO example, final IslandOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractIslandVO, IslandOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.island.selectByExample", dwo);
  }

  public Cursor<app.daos.IslandVO> selectCursor(final app.daos.primitives.AbstractIslandVO example, final IslandOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractIslandVO, IslandOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.IslandVO>(this.sqlSession.selectCursor("mappers.island.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.IslandVO> select(final IslandDAO.IslandTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.IslandVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.island.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.IslandVO insert(final app.daos.primitives.AbstractIslandVO vo) {
    String id = "mappers.island.insert";
    this.sqlSession.insert(id, vo);
    app.daos.IslandVO mo = new app.daos.IslandVO();
    mo.setId(vo.getId());
    mo.setSegment(vo.getSegment());
    mo.setXStart(vo.getXStart());
    mo.setXEnd(vo.getXEnd());
    mo.setHeight(vo.getHeight());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.primitives.AbstractIslandVO example, final app.daos.primitives.AbstractIslandVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractIslandVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractIslandVO>(example, updateValues);
    return this.sqlSession.update("mappers.island.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractIslandVO updateValues, final IslandDAO.IslandTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getSegment() != null) values.put("segment", updateValues.getSegment());
    if (updateValues.getXStart() != null) values.put("x_start", updateValues.getXStart());
    if (updateValues.getXEnd() != null) values.put("x_end", updateValues.getXEnd());
    if (updateValues.getHeight() != null) values.put("height", updateValues.getHeight());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.island.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractIslandVO example) {
    return this.sqlSession.delete("mappers.island.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final IslandDAO.IslandTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.island.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum IslandOrderBy implements OrderBy {

    ID("public.island", "id", true), //
    ID$DESC("public.island", "id", false), //
    SEGMENT("public.island", "segment", true), //
    SEGMENT$DESC("public.island", "segment", false), //
    X_START("public.island", "x_start", true), //
    X_START$DESC("public.island", "x_start", false), //
    X_END("public.island", "x_end", true), //
    X_END$DESC("public.island", "x_end", false), //
    HEIGHT("public.island", "height", true), //
    HEIGHT$DESC("public.island", "height", false);

    private IslandOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static IslandTable newTable() {
    return new IslandTable();
  }

  public static IslandTable newTable(final String alias) {
    return new IslandTable(alias);
  }

  public static class IslandTable extends Table {

    // Properties

    public NumberColumn id;
    public NumberColumn segment;
    public NumberColumn xStart;
    public NumberColumn xEnd;
    public NumberColumn height;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.segment, this.xStart, this.xEnd, this.height);
    }

    // Constructors

    IslandTable() {
      super(null, "public", "island", "Table", null);
      initialize();
    }

    IslandTable(final String alias) {
      super(null, "public", "island", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "int4", 10, 0);
      this.segment = new NumberColumn(this, "segment", "segment", "int4", 10, 0);
      this.xStart = new NumberColumn(this, "x_start", "xStart", "int4", 10, 0);
      this.xEnd = new NumberColumn(this, "x_end", "xEnd", "int4", 10, 0);
      this.height = new NumberColumn(this, "height", "height", "int4", 10, 0);
    }

  }

}
