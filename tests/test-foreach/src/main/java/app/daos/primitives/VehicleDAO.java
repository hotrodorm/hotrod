// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractVehicleVO;
import app.daos.VehicleVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class VehicleDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.VehicleVO select(final java.lang.Integer id) {
    if (id == null)
      return null;
    app.daos.VehicleVO vo = new app.daos.VehicleVO();
    vo.setId(id);
    return this.sqlSession.selectOne("mappers.vehicle.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.VehicleVO> select(final app.daos.primitives.AbstractVehicleVO example, final VehicleOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractVehicleVO, VehicleOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.vehicle.selectByExample", dwo);
  }

  public Cursor<app.daos.VehicleVO> selectCursor(final app.daos.primitives.AbstractVehicleVO example, final VehicleOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractVehicleVO, VehicleOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.VehicleVO>(this.sqlSession.selectCursor("mappers.vehicle.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.VehicleVO> select(final VehicleDAO.VehicleTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.VehicleVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.vehicle.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.VehicleVO insert(final app.daos.primitives.AbstractVehicleVO vo) {
    return insert(vo, false);
  }

  public app.daos.VehicleVO insert(final app.daos.primitives.AbstractVehicleVO vo, final boolean retrieveDefaults) {
    String id = retrieveDefaults ? "mappers.vehicle.insertRetrievingDefaults" : "mappers.vehicle.insert";
    int rows = this.sqlSession.insert(id, vo);
    app.daos.VehicleVO mo = new app.daos.VehicleVO();
    mo.setId(vo.getId());
    mo.setName(vo.getName());
    mo.setMileage(vo.getMileage());
    mo.setVersionNumber(vo.getVersionNumber());
    mo.setVtype(vo.getVtype());
    return mo;
  }

  // update by PK

  public int update(final app.daos.VehicleVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.update("mappers.vehicle.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Integer id) {
    if (id == null) return 0;
    app.daos.VehicleVO vo = new app.daos.VehicleVO();
    vo.setId(id);
    if (vo.id == null) return 0;
    return this.sqlSession.delete("mappers.vehicle.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.AbstractVehicleVO example, final app.daos.primitives.AbstractVehicleVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractVehicleVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractVehicleVO>(example, updateValues);
    return this.sqlSession.update("mappers.vehicle.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractVehicleVO updateValues, final VehicleDAO.VehicleTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getName() != null) values.put("name", updateValues.getName());
    if (updateValues.getMileage() != null) values.put("mileage", updateValues.getMileage());
    if (updateValues.getVersionNumber() != null) values.put("version_number", updateValues.getVersionNumber());
    if (updateValues.getVtype() != null) values.put("vtype", updateValues.getVtype());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.vehicle.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractVehicleVO example) {
    return this.sqlSession.delete("mappers.vehicle.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final VehicleDAO.VehicleTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.vehicle.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum VehicleOrderBy implements OrderBy {

    ID("public.vehicle", "id", true), //
    ID$DESC("public.vehicle", "id", false), //
    NAME("public.vehicle", "name", true), //
    NAME$DESC("public.vehicle", "name", false), //
    NAME$CASEINSENSITIVE("public.vehicle", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("public.vehicle", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("public.vehicle", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("public.vehicle", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.vehicle", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.vehicle", "lower(name), name", true), //
    MILEAGE("public.vehicle", "mileage", true), //
    MILEAGE$DESC("public.vehicle", "mileage", false), //
    VERSION_NUMBER("public.vehicle", "version_number", true), //
    VERSION_NUMBER$DESC("public.vehicle", "version_number", false), //
    VTYPE("public.vehicle", "vtype", true), //
    VTYPE$DESC("public.vehicle", "vtype", false);

    private VehicleOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static VehicleTable newTable() {
    return new VehicleTable();
  }

  public static VehicleTable newTable(final String alias) {
    return new VehicleTable(alias);
  }

  public static class VehicleTable extends Table {

    // Properties

    public NumberColumn id;
    public StringColumn name;
    public NumberColumn mileage;
    public NumberColumn versionNumber;
    public NumberColumn vtype;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.name, this.mileage, this.versionNumber, this.vtype);
    }

    // Constructors

    VehicleTable() {
      super(null, "public", "vehicle", "Table", null);
      initialize();
    }

    VehicleTable(final String alias) {
      super(null, "public", "vehicle", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "serial", 10, 0);
      this.name = new StringColumn(this, "name", "name", "varchar", 40, 0);
      this.mileage = new NumberColumn(this, "mileage", "mileage", "int4", 10, 0);
      this.versionNumber = new NumberColumn(this, "version_number", "versionNumber", "int2", 5, 0);
      this.vtype = new NumberColumn(this, "vtype", "vtype", "int4", 10, 0);
    }

  }

}
