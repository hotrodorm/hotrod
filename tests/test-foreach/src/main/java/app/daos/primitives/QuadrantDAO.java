// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractQuadrantVO;
import app.daos.QuadrantVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class QuadrantDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.QuadrantVO select(final java.lang.Integer region, final java.lang.Integer area) {
    if (region == null)
      return null;
    if (area == null)
      return null;
    app.daos.QuadrantVO vo = new app.daos.QuadrantVO();
    vo.setRegion(region);
    vo.setArea(area);
    return this.sqlSession.selectOne("mappers.quadrant.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.QuadrantVO> select(final app.daos.primitives.AbstractQuadrantVO example, final QuadrantOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractQuadrantVO, QuadrantOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.quadrant.selectByExample", dwo);
  }

  public Cursor<app.daos.QuadrantVO> selectCursor(final app.daos.primitives.AbstractQuadrantVO example, final QuadrantOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractQuadrantVO, QuadrantOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.QuadrantVO>(this.sqlSession.selectCursor("mappers.quadrant.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.QuadrantVO> select(final QuadrantDAO.QuadrantTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.QuadrantVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.quadrant.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.QuadrantVO insert(final app.daos.primitives.AbstractQuadrantVO vo) {
    String id = "mappers.quadrant.insert";
    this.sqlSession.insert(id, vo);
    app.daos.QuadrantVO mo = new app.daos.QuadrantVO();
    mo.setRegion(vo.getRegion());
    mo.setArea(vo.getArea());
    mo.setCaption(vo.getCaption());
    return mo;
  }

  // update by PK

  public int update(final app.daos.QuadrantVO vo) {
    if (vo.region == null) return 0;
    if (vo.area == null) return 0;
    return this.sqlSession.update("mappers.quadrant.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Integer region, final java.lang.Integer area) {
    if (region == null) return 0;
    if (area == null) return 0;
    app.daos.QuadrantVO vo = new app.daos.QuadrantVO();
    vo.setRegion(region);
    vo.setArea(area);
    if (vo.region == null) return 0;
    if (vo.area == null) return 0;
    return this.sqlSession.delete("mappers.quadrant.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.AbstractQuadrantVO example, final app.daos.primitives.AbstractQuadrantVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractQuadrantVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractQuadrantVO>(example, updateValues);
    return this.sqlSession.update("mappers.quadrant.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractQuadrantVO updateValues, final QuadrantDAO.QuadrantTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getRegion() != null) values.put("region", updateValues.getRegion());
    if (updateValues.getArea() != null) values.put("area", updateValues.getArea());
    if (updateValues.getCaption() != null) values.put("caption", updateValues.getCaption());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.quadrant.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractQuadrantVO example) {
    return this.sqlSession.delete("mappers.quadrant.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final QuadrantDAO.QuadrantTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.quadrant.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum QuadrantOrderBy implements OrderBy {

    REGION("public.quadrant", "region", true), //
    REGION$DESC("public.quadrant", "region", false), //
    AREA("public.quadrant", "area", true), //
    AREA$DESC("public.quadrant", "area", false), //
    CAPTION("public.quadrant", "caption", true), //
    CAPTION$DESC("public.quadrant", "caption", false), //
    CAPTION$CASEINSENSITIVE("public.quadrant", "lower(caption)", true), //
    CAPTION$CASEINSENSITIVE_STABLE_FORWARD("public.quadrant", "lower(caption), caption", true), //
    CAPTION$CASEINSENSITIVE_STABLE_REVERSE("public.quadrant", "lower(caption), caption", false), //
    CAPTION$DESC_CASEINSENSITIVE("public.quadrant", "lower(caption)", false), //
    CAPTION$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.quadrant", "lower(caption), caption", false), //
    CAPTION$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.quadrant", "lower(caption), caption", true);

    private QuadrantOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static QuadrantTable newTable() {
    return new QuadrantTable();
  }

  public static QuadrantTable newTable(final String alias) {
    return new QuadrantTable(alias);
  }

  public static class QuadrantTable extends Table {

    // Properties

    public NumberColumn region;
    public NumberColumn area;
    public StringColumn caption;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.region, this.area, this.caption);
    }

    // Constructors

    QuadrantTable() {
      super(null, "public", "quadrant", "Table", null);
      initialize();
    }

    QuadrantTable(final String alias) {
      super(null, "public", "quadrant", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.region = new NumberColumn(this, "region", "region", "int4", 10, 0);
      this.area = new NumberColumn(this, "area", "area", "int4", 10, 0);
      this.caption = new StringColumn(this, "caption", "caption", "varchar", 20, 0);
    }

  }

}
