// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractHolidayVO;
import app.daos.HolidayVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class HolidayDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.HolidayVO> select(final app.daos.primitives.AbstractHolidayVO example, final HolidayOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractHolidayVO, HolidayOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.holiday.selectByExample", dwo);
  }

  public Cursor<app.daos.HolidayVO> selectCursor(final app.daos.primitives.AbstractHolidayVO example, final HolidayOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractHolidayVO, HolidayOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.HolidayVO>(this.sqlSession.selectCursor("mappers.holiday.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.HolidayVO> select(final HolidayDAO.HolidayTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.HolidayVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.holiday.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.HolidayVO insert(final app.daos.primitives.AbstractHolidayVO vo) {
    String id = "mappers.holiday.insert";
    this.sqlSession.insert(id, vo);
    app.daos.HolidayVO mo = new app.daos.HolidayVO();
    mo.setD(vo.getD());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.primitives.AbstractHolidayVO example, final app.daos.primitives.AbstractHolidayVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractHolidayVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractHolidayVO>(example, updateValues);
    return this.sqlSession.update("mappers.holiday.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractHolidayVO updateValues, final HolidayDAO.HolidayTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getD() != null) values.put("d", updateValues.getD());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.holiday.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractHolidayVO example) {
    return this.sqlSession.delete("mappers.holiday.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final HolidayDAO.HolidayTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.holiday.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum HolidayOrderBy implements OrderBy {

    D("public.holiday", "d", true), //
    D$DESC("public.holiday", "d", false);

    private HolidayOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static HolidayTable newTable() {
    return new HolidayTable();
  }

  public static HolidayTable newTable(final String alias) {
    return new HolidayTable(alias);
  }

  public static class HolidayTable extends Table {

    // Properties

    public DateTimeColumn d;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.d);
    }

    // Constructors

    HolidayTable() {
      super(null, "public", "holiday", "Table", null);
      initialize();
    }

    HolidayTable(final String alias) {
      super(null, "public", "holiday", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.d = new DateTimeColumn(this, "d", "d", "date", 13, 0);
    }

  }

}
