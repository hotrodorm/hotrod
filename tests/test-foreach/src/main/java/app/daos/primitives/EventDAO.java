// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractEventVO;
import app.daos.EventVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class EventDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.EventVO select(final java.lang.Long id) {
    if (id == null)
      return null;
    app.daos.EventVO vo = new app.daos.EventVO();
    vo.setId(id);
    return this.sqlSession.selectOne("mappers.event.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.EventVO> select(final app.daos.primitives.AbstractEventVO example, final EventOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractEventVO, EventOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.event.selectByExample", dwo);
  }

  public Cursor<app.daos.EventVO> selectCursor(final app.daos.primitives.AbstractEventVO example, final EventOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractEventVO, EventOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.EventVO>(this.sqlSession.selectCursor("mappers.event.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.EventVO> select(final EventDAO.EventTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.EventVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.event.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.EventVO insert(final app.daos.primitives.AbstractEventVO vo) {
    String id = "mappers.event.insert";
    this.sqlSession.insert(id, vo);
    app.daos.EventVO mo = new app.daos.EventVO();
    mo.setId(vo.getId());
    mo.setName(vo.getName());
    mo.setStatus(vo.getStatus());
    return mo;
  }

  // update by PK

  public int update(final app.daos.EventVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.update("mappers.event.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Long id) {
    if (id == null) return 0;
    app.daos.EventVO vo = new app.daos.EventVO();
    vo.setId(id);
    if (vo.id == null) return 0;
    return this.sqlSession.delete("mappers.event.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.AbstractEventVO example, final app.daos.primitives.AbstractEventVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractEventVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractEventVO>(example, updateValues);
    return this.sqlSession.update("mappers.event.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractEventVO updateValues, final EventDAO.EventTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getName() != null) values.put("name", updateValues.getName());
    if (updateValues.getStatus() != null) values.put("status", updateValues.getStatus());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.event.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractEventVO example) {
    return this.sqlSession.delete("mappers.event.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final EventDAO.EventTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.event.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum EventOrderBy implements OrderBy {

    ID("public.event", "id", true), //
    ID$DESC("public.event", "id", false), //
    NAME("public.event", "name", true), //
    NAME$DESC("public.event", "name", false), //
    NAME$CASEINSENSITIVE("public.event", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("public.event", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("public.event", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("public.event", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.event", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.event", "lower(name), name", true), //
    STATUS("public.event", "status", true), //
    STATUS$DESC("public.event", "status", false);

    private EventOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static EventTable newTable() {
    return new EventTable();
  }

  public static EventTable newTable(final String alias) {
    return new EventTable(alias);
  }

  public static class EventTable extends Table {

    // Properties

    public NumberColumn id;
    public StringColumn name;
    public NumberColumn status;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.name, this.status);
    }

    // Constructors

    EventTable() {
      super(null, "public", "event", "Table", null);
      initialize();
    }

    EventTable(final String alias) {
      super(null, "public", "event", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "int8", 19, 0);
      this.name = new StringColumn(this, "name", "name", "varchar", 20, 0);
      this.status = new NumberColumn(this, "status", "status", "int4", 10, 0);
    }

  }

}
