// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractCodesVO;
import app.daos.CodesVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class CodesDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes

  public app.daos.CodesVO selectByUIAccount_VersionName(final java.lang.Integer account, final java.lang.Integer versionName) {
    if (account == null)
      return null;
    if (versionName == null)
      return null;
    app.daos.CodesVO vo = new app.daos.CodesVO();
    vo.setAccount(account);
    vo.setVersionName(versionName);
    return this.sqlSession.selectOne("mappers.codes.selectByUIAccount_VersionName", vo);
  }

  public app.daos.CodesVO selectByUIAccountVersion_Name(final java.lang.Integer accountVersion, final java.lang.Integer name) {
    if (accountVersion == null)
      return null;
    if (name == null)
      return null;
    app.daos.CodesVO vo = new app.daos.CodesVO();
    vo.setAccountVersion(accountVersion);
    vo.setName(name);
    return this.sqlSession.selectOne("mappers.codes.selectByUIAccountVersion_Name", vo);
  }

  // select by example

  public List<app.daos.CodesVO> select(final app.daos.primitives.AbstractCodesVO example, final CodesOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractCodesVO, CodesOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.codes.selectByExample", dwo);
  }

  public Cursor<app.daos.CodesVO> selectCursor(final app.daos.primitives.AbstractCodesVO example, final CodesOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractCodesVO, CodesOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.CodesVO>(this.sqlSession.selectCursor("mappers.codes.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.CodesVO> select(final CodesDAO.CodesTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.CodesVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.codes.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.CodesVO insert(final app.daos.primitives.AbstractCodesVO vo) {
    return insert(vo, false);
  }

  public app.daos.CodesVO insert(final app.daos.primitives.AbstractCodesVO vo, final boolean retrieveDefaults) {
    String id = retrieveDefaults ? "mappers.codes.insertRetrievingDefaults" : "mappers.codes.insert";
    int rows = this.sqlSession.insert(id, vo);
    app.daos.CodesVO mo = new app.daos.CodesVO();
    mo.setId(vo.getId());
    mo.setAccount(vo.getAccount());
    mo.setVersionName(vo.getVersionName());
    mo.setAccountVersion(vo.getAccountVersion());
    mo.setName(vo.getName());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.primitives.AbstractCodesVO example, final app.daos.primitives.AbstractCodesVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractCodesVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractCodesVO>(example, updateValues);
    return this.sqlSession.update("mappers.codes.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractCodesVO updateValues, final CodesDAO.CodesTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getAccount() != null) values.put("account", updateValues.getAccount());
    if (updateValues.getVersionName() != null) values.put("version_name", updateValues.getVersionName());
    if (updateValues.getAccountVersion() != null) values.put("account_version", updateValues.getAccountVersion());
    if (updateValues.getName() != null) values.put("name", updateValues.getName());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.codes.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractCodesVO example) {
    return this.sqlSession.delete("mappers.codes.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final CodesDAO.CodesTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.codes.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum CodesOrderBy implements OrderBy {

    ID("public.codes", "id", true), //
    ID$DESC("public.codes", "id", false), //
    ACCOUNT("public.codes", "account", true), //
    ACCOUNT$DESC("public.codes", "account", false), //
    VERSION_NAME("public.codes", "version_name", true), //
    VERSION_NAME$DESC("public.codes", "version_name", false), //
    ACCOUNT_VERSION("public.codes", "account_version", true), //
    ACCOUNT_VERSION$DESC("public.codes", "account_version", false), //
    NAME("public.codes", "name", true), //
    NAME$DESC("public.codes", "name", false);

    private CodesOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static CodesTable newTable() {
    return new CodesTable();
  }

  public static CodesTable newTable(final String alias) {
    return new CodesTable(alias);
  }

  public static class CodesTable extends Table {

    // Properties

    public NumberColumn id;
    public NumberColumn account;
    public NumberColumn versionName;
    public NumberColumn accountVersion;
    public NumberColumn name;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.account, this.versionName, this.accountVersion, this.name);
    }

    // Constructors

    CodesTable() {
      super(null, "public", "codes", "Table", null);
      initialize();
    }

    CodesTable(final String alias) {
      super(null, "public", "codes", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "serial", 10, 0);
      this.account = new NumberColumn(this, "account", "account", "int4", 10, 0);
      this.versionName = new NumberColumn(this, "version_name", "versionName", "int4", 10, 0);
      this.accountVersion = new NumberColumn(this, "account_version", "accountVersion", "int4", 10, 0);
      this.name = new NumberColumn(this, "name", "name", "int4", 10, 0);
    }

  }

}
