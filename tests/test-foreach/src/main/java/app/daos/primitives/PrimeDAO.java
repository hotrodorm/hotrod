// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractPrimeVO;
import app.daos.PrimeVO;

import java.util.Map;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class PrimeDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.PrimeVO select(final java.lang.Integer id) {
    if (id == null)
      return null;
    app.daos.PrimeVO vo = new app.daos.PrimeVO();
    vo.setId(id);
    return this.sqlSession.selectOne("mappers.prime.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.PrimeVO> select(final app.daos.primitives.AbstractPrimeVO example, final PrimeOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractPrimeVO, PrimeOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.prime.selectByExample", dwo);
  }

  public Cursor<app.daos.PrimeVO> selectCursor(final app.daos.primitives.AbstractPrimeVO example, final PrimeOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractPrimeVO, PrimeOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.PrimeVO>(this.sqlSession.selectCursor("mappers.prime.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.PrimeVO> select(final PrimeDAO.PrimeTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.PrimeVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.prime.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.PrimeVO insert(final app.daos.primitives.AbstractPrimeVO vo) {
    String id = "mappers.prime.insert";
    this.sqlSession.insert(id, vo);
    app.daos.PrimeVO mo = new app.daos.PrimeVO();
    mo.setId(vo.getId());
    mo.setAmount(vo.getAmount());
    mo.setN(vo.getN());
    return mo;
  }

  // update by PK

  public int update(final app.daos.PrimeVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.update("mappers.prime.updateByPK", vo);
  }

  // delete by PK

  public int delete(final java.lang.Integer id) {
    if (id == null) return 0;
    app.daos.PrimeVO vo = new app.daos.PrimeVO();
    vo.setId(id);
    if (vo.id == null) return 0;
    return this.sqlSession.delete("mappers.prime.deleteByPK", vo);
  }

  // update by example

  public int update(final app.daos.primitives.AbstractPrimeVO example, final app.daos.primitives.AbstractPrimeVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractPrimeVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractPrimeVO>(example, updateValues);
    return this.sqlSession.update("mappers.prime.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractPrimeVO updateValues, final PrimeDAO.PrimeTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getId() != null) values.put("id", updateValues.getId());
    if (updateValues.getAmount() != null) values.put("amount", updateValues.getAmount());
    if (updateValues.getN() != null) values.put("n", updateValues.getN());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.prime.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractPrimeVO example) {
    return this.sqlSession.delete("mappers.prime.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final PrimeDAO.PrimeTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.prime.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum PrimeOrderBy implements OrderBy {

    ID("public.prime", "id", true), //
    ID$DESC("public.prime", "id", false), //
    AMOUNT("public.prime", "amount", true), //
    AMOUNT$DESC("public.prime", "amount", false), //
    N("public.prime", "n", true), //
    N$DESC("public.prime", "n", false);

    private PrimeOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static PrimeTable newTable() {
    return new PrimeTable();
  }

  public static PrimeTable newTable(final String alias) {
    return new PrimeTable(alias);
  }

  public static class PrimeTable extends Table {

    // Properties

    public NumberColumn id;
    public NumberColumn amount;
    public ObjectColumn n;

    // Getters

    public AllColumns star() {
      return new AllColumns(this, this.id, this.amount, this.n);
    }

    // Constructors

    PrimeTable() {
      super(null, "public", "prime", "Table", null);
      initialize();
    }

    PrimeTable(final String alias) {
      super(null, "public", "prime", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "id", "id", "int4", 10, 0);
      this.amount = new NumberColumn(this, "amount", "amount", "int4", 10, 0);
      this.n = new ObjectColumn(this, "n", "n", "_int4", 10, 0);
    }

  }

}
