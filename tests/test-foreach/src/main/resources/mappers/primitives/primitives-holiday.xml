<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.holiday">

  <!-- columns -->

  <sql id="columns">
    d
  </sql>

  <sql id="columnsWithAlias">
    ${alias}.d
  </sql>

  <resultMap id="allColumns" type="app.daos.HolidayVO">
    <result property="d" column="d" />
  </resultMap>

  <!-- select by PK: no mapping generated, since the table does not have a PK -->

  <!-- select by unique indexes: no mappings generated, since the table does not have UIs -->

  <!-- select by example -->

  <select id="selectByExample" resultMap="allColumns">
    select
      <include refid="columns" />
     from public.holiday
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="p.d != null">
        and d = #{p.d,jdbcType=DATE}
      </if>
    </trim>
    <if test="o != null">
      order by ${o}
    </if>
  </select>

  <!-- select by criteria -->

  <select id="selectByCriteria" resultMap="allColumns">
    ${sql}
  </select>

  <!-- insert (no auto-generated columns) -->

  <insert id="insert">
    insert into public.holiday (
      <trim prefixOverrides=", ">
        <if test="true">, d</if>
      </trim>
      ) values (
      <trim prefixOverrides=", ">
        <if test="true">, #{d,jdbcType=DATE}</if>
      </trim>
      )
  </insert>

  <!-- update by PK: no update mapping generated, since the table does not have a PK -->

  <!-- delete by PK: no delete mapping generated, since the table does not have a PK -->

  <!-- update by example -->

  <update id="updateByExample">
    update public.holiday
    <set>
      <if test="values.d != null">d = #{values.d,jdbcType=DATE},</if>
    </set>
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="filter.d != null">
        and d = #{filter.d,jdbcType=DATE}
      </if>
    </trim>
  </update>

  <!-- update by criteria -->

  <select id="updateByCriteria">
    ${sql}
  </select>

  <!-- delete by example -->

  <delete id="deleteByExample">
    delete from public.holiday
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="d != null">
        and d = #{d,jdbcType=DATE}
      </if>
    </trim>
  </delete>

  <!-- delete by criteria -->

  <select id="deleteByCriteria">
    ${sql}
  </select>

</mapper>
