<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.quadrant">

  <!-- columns -->

  <sql id="columns">
    region,
    area,
    caption
  </sql>

  <sql id="columnsWithAlias">
    ${alias}.region,
    ${alias}.area,
    ${alias}.caption
  </sql>

  <resultMap id="allColumns" type="app.daos.QuadrantVO">
    <id property="region" column="region" />
    <id property="area" column="area" />
    <result property="caption" column="caption" />
  </resultMap>

  <!-- select by PK -->

  <select id="selectByPK" resultMap="allColumns">
    select
      <include refid="columns" />
     from public.quadrant
     where
      region = #{region,jdbcType=INTEGER}
      and area = #{area,jdbcType=INTEGER}
  </select>

  <!-- no select by unique indexes since there are no UI (besides the PK) -->

  <!-- select by example -->

  <select id="selectByExample" resultMap="allColumns">
    select
      <include refid="columns" />
     from public.quadrant
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="p.region != null">
        and region = #{p.region,jdbcType=INTEGER}
      </if>
      <if test="p.area != null">
        and area = #{p.area,jdbcType=INTEGER}
      </if>
      <if test="p.caption != null">
        and caption = #{p.caption,jdbcType=VARCHAR}
      </if>
    </trim>
    <if test="o != null">
      order by ${o}
    </if>
  </select>

  <!-- select by criteria -->

  <select id="selectByCriteria" resultMap="allColumns">
    ${sql}
  </select>

  <!-- insert (no auto-generated columns) -->

  <insert id="insert">
    insert into public.quadrant (
      <trim prefixOverrides=", ">
        <if test="true">, region</if>
        <if test="true">, area</if>
        <if test="true">, caption</if>
      </trim>
      ) values (
      <trim prefixOverrides=", ">
        <if test="true">, #{region,jdbcType=INTEGER}</if>
        <if test="true">, #{area,jdbcType=INTEGER}</if>
        <if test="true">, #{caption,jdbcType=VARCHAR}</if>
      </trim>
      )
  </insert>

  <!-- update by PK -->

  <update id="updateByPK">
    update public.quadrant set
      caption = #{caption,jdbcType=VARCHAR}
     where
      region = #{region,jdbcType=INTEGER}
      and area = #{area,jdbcType=INTEGER}
  </update>

  <!-- delete by PK -->

  <delete id="deleteByPK">
    delete from public.quadrant
     where
      region = #{region,jdbcType=INTEGER}
      and area = #{area,jdbcType=INTEGER}
  </delete>

  <!-- update by example -->

  <update id="updateByExample">
    update public.quadrant
    <set>
      <if test="values.region != null">region = #{values.region,jdbcType=INTEGER},</if>
      <if test="values.area != null">area = #{values.area,jdbcType=INTEGER},</if>
      <if test="values.caption != null">caption = #{values.caption,jdbcType=VARCHAR},</if>
    </set>
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="filter.region != null">
        and region = #{filter.region,jdbcType=INTEGER}
      </if>
      <if test="filter.area != null">
        and area = #{filter.area,jdbcType=INTEGER}
      </if>
      <if test="filter.caption != null">
        and caption = #{filter.caption,jdbcType=VARCHAR}
      </if>
    </trim>
  </update>

  <!-- update by criteria -->

  <select id="updateByCriteria">
    ${sql}
  </select>

  <!-- delete by example -->

  <delete id="deleteByExample">
    delete from public.quadrant
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="region != null">
        and region = #{region,jdbcType=INTEGER}
      </if>
      <if test="area != null">
        and area = #{area,jdbcType=INTEGER}
      </if>
      <if test="caption != null">
        and caption = #{caption,jdbcType=VARCHAR}
      </if>
    </trim>
  </delete>

  <!-- delete by criteria -->

  <select id="deleteByCriteria">
    ${sql}
  </select>

</mapper>
