<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.testMixed1">

  <!-- columns -->

  <sql id="columns">
    id,
    name,
    extra_id1,
    extra_id2
  </sql>

  <sql id="columnsWithAlias">
    ${alias}.id,
    ${alias}.name,
    ${alias}.extra_id1,
    ${alias}.extra_id2
  </sql>

  <resultMap id="allColumns" type="app.daos.TestMixed1VO">
    <result property="id" column="id" />
    <result property="name" column="name" />
    <result property="extraId1" column="extra_id1" />
    <result property="extraId2" column="extra_id2" />
  </resultMap>

  <!-- select by PK: no mapping generated, since the table does not have a PK -->

  <!-- select by unique indexes: no mappings generated, since the table does not have UIs -->

  <!-- select by example -->

  <select id="selectByExample" resultMap="allColumns">
    select
      <include refid="columns" />
     from public.test_mixed1
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="p.id != null">
        and id = #{p.id,jdbcType=INTEGER}
      </if>
      <if test="p.name != null">
        and name = #{p.name,jdbcType=VARCHAR}
      </if>
      <if test="p.extraId1 != null">
        and extra_id1 = #{p.extraId1,jdbcType=INTEGER}
      </if>
      <if test="p.extraId2 != null">
        and extra_id2 = #{p.extraId2,jdbcType=INTEGER}
      </if>
    </trim>
    <if test="o != null">
      order by ${o}
    </if>
  </select>

  <!-- select by criteria -->

  <select id="selectByCriteria" resultMap="allColumns">
    ${sql}
  </select>

  <!-- insert (does not integrate sequences, and integrates identities) -->

  <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    insert into public.test_mixed1 (
      <trim prefixOverrides=", ">
        <if test="id != null">, id</if>
        <if test="true">, name</if>
        <if test="true">, extra_id1</if>
        <if test="true">, extra_id2</if>
      </trim>
      ) values (
      <trim prefixOverrides=", ">
        <if test="id != null">, #{id,jdbcType=INTEGER}</if>
        <if test="true">, #{name,jdbcType=VARCHAR}</if>
        <if test="true">, #{extraId1,jdbcType=INTEGER}</if>
        <if test="true">, #{extraId2,jdbcType=INTEGER}</if>
      </trim>
      )
  </insert>

  <!-- update by PK: no update mapping generated, since the table does not have a PK -->

  <!-- delete by PK: no delete mapping generated, since the table does not have a PK -->

  <!-- update by example -->

  <update id="updateByExample">
    update public.test_mixed1
    <set>
      <if test="values.id != null">id = #{values.id,jdbcType=INTEGER},</if>
      <if test="values.name != null">name = #{values.name,jdbcType=VARCHAR},</if>
      <if test="values.extraId1 != null">extra_id1 = #{values.extraId1,jdbcType=INTEGER},</if>
      <if test="values.extraId2 != null">extra_id2 = #{values.extraId2,jdbcType=INTEGER},</if>
    </set>
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="filter.id != null">
        and id = #{filter.id,jdbcType=INTEGER}
      </if>
      <if test="filter.name != null">
        and name = #{filter.name,jdbcType=VARCHAR}
      </if>
      <if test="filter.extraId1 != null">
        and extra_id1 = #{filter.extraId1,jdbcType=INTEGER}
      </if>
      <if test="filter.extraId2 != null">
        and extra_id2 = #{filter.extraId2,jdbcType=INTEGER}
      </if>
    </trim>
  </update>

  <!-- update by criteria -->

  <select id="updateByCriteria">
    ${sql}
  </select>

  <!-- delete by example -->

  <delete id="deleteByExample">
    delete from public.test_mixed1
    <trim prefix="where&#xA;" prefixOverrides="and |or ">
      <if test="id != null">
        and id = #{id,jdbcType=INTEGER}
      </if>
      <if test="name != null">
        and name = #{name,jdbcType=VARCHAR}
      </if>
      <if test="extraId1 != null">
        and extra_id1 = #{extraId1,jdbcType=INTEGER}
      </if>
      <if test="extraId2 != null">
        and extra_id2 = #{extraId2,jdbcType=INTEGER}
      </if>
    </trim>
  </delete>

  <!-- delete by criteria -->

  <select id="deleteByCriteria">
    ${sql}
  </select>

</mapper>
