// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractPaymentVO;
import app.daos.PaymentVO;

import java.lang.Override;
import java.util.Map;
import java.util.ArrayList;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.spring.SpringBeanObjectFactory;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.util.CastUtil;
import org.hotrod.runtime.livesql.metadata.Column;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class PaymentDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  @Autowired
  private SpringBeanObjectFactory springBeanObjectFactory;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
    this.sqlSession.getConfiguration().setObjectFactory(this.springBeanObjectFactory);
  }

  // Row Parser

  public app.daos.PaymentVO parseRow(Map<String, Object> m) {
    return parseRow(m, null, null);
  }

  public app.daos.PaymentVO parseRow(Map<String, Object> m, String prefix) {
    return parseRow(m, prefix, null);
  }

  public app.daos.PaymentVO parseRow(Map<String, Object> m, String prefix, String suffix) {
    app.daos.PaymentVO mo = this.applicationContext.getBean(app.daos.PaymentVO.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    mo.setPaymentDate((java.sql.Date) m.get(p + "paymentDate" + s));
    mo.setInvoiceId(CastUtil.toInteger((Number) m.get(p + "invoiceId" + s)));
    mo.setAmount(CastUtil.toInteger((Number) m.get(p + "amount" + s)));
    return mo;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.PaymentVO> select(final app.daos.primitives.AbstractPaymentVO example, final PaymentOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractPaymentVO, PaymentOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.payment.selectByExample", dwo);
  }

  public Cursor<app.daos.PaymentVO> selectCursor(final app.daos.primitives.AbstractPaymentVO example, final PaymentOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractPaymentVO, PaymentOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.PaymentVO>(this.sqlSession.selectCursor("mappers.payment.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.PaymentVO> select(final PaymentDAO.PaymentTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.PaymentVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "mappers.payment.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.PaymentVO insert(final app.daos.primitives.AbstractPaymentVO vo) {
    String id = "mappers.payment.insert";
    this.sqlSession.insert(id, vo);
    app.daos.PaymentVO mo = new app.daos.PaymentVO();
    mo.setPaymentDate(vo.getPaymentDate());
    mo.setInvoiceId(vo.getInvoiceId());
    mo.setAmount(vo.getAmount());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.primitives.AbstractPaymentVO example, final app.daos.primitives.AbstractPaymentVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractPaymentVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractPaymentVO>(example, updateValues);
    return this.sqlSession.update("mappers.payment.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.primitives.AbstractPaymentVO updateValues, final PaymentDAO.PaymentTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getPaymentDate() != null) values.put("payment_date", updateValues.getPaymentDate());
    if (updateValues.getInvoiceId() != null) values.put("invoice_id", updateValues.getInvoiceId());
    if (updateValues.getAmount() != null) values.put("amount", updateValues.getAmount());
    return new UpdateSetCompletePhase(tableOrView, this.liveSQLDialect, this.sqlSession,
      "mappers.payment.updateByCriteria", predicate, values);
  }


  // delete by example

  public int delete(final app.daos.primitives.AbstractPaymentVO example) {
    return this.sqlSession.delete("mappers.payment.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final PaymentDAO.PaymentTable from, final Predicate predicate) {
    return new DeleteWherePhase(from, this.liveSQLDialect, this.sqlSession,
      "mappers.payment.deleteByCriteria", predicate);
  }

  // DAO ordering

  public enum PaymentOrderBy implements OrderBy {

    PAYMENT_DATE("public.payment", "payment_date", true), //
    PAYMENT_DATE$DESC("public.payment", "payment_date", false), //
    INVOICE_ID("public.payment", "invoice_id", true), //
    INVOICE_ID$DESC("public.payment", "invoice_id", false), //
    AMOUNT("public.payment", "amount", true), //
    AMOUNT$DESC("public.payment", "amount", false);

    private PaymentOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static PaymentTable newTable() {
    return new PaymentTable();
  }

  public static PaymentTable newTable(final String alias) {
    return new PaymentTable(alias);
  }

  public static class PaymentTable extends Table {

    // Properties

    public DateTimeColumn paymentDate;
    public NumberColumn invoiceId;
    public NumberColumn amount;

    // Getters

    public AllColumns star() {
      return new AllColumns(this.paymentDate, this.invoiceId, this.amount);
    }

    // Constructors

    PaymentTable() {
      super(null, "PUBLIC", "PAYMENT", "Table", null);
      initialize();
    }

    PaymentTable(final String alias) {
      super(null, "PUBLIC", "PAYMENT", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      super.columns = new ArrayList<>();
      this.paymentDate = new DateTimeColumn(this, "PAYMENT_DATE", "paymentDate", "DATE", 10, 0);
      super.columns.add(this.paymentDate);
      this.invoiceId = new NumberColumn(this, "INVOICE_ID", "invoiceId", "INTEGER", 32, 0);
      super.columns.add(this.invoiceId);
      this.amount = new NumberColumn(this, "AMOUNT", "amount", "INTEGER", 32, 0);
      super.columns.add(this.amount);
    }

  }

}
