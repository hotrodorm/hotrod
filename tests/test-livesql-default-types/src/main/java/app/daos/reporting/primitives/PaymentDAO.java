// Autogenerated by HotRod -- Do not edit.

package app.daos.reporting.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.reporting.primitives.AbstractPaymentVO;
import app.daos.reporting.PaymentVO;

import java.lang.Override;
import java.util.Map;
import java.util.ArrayList;
import java.util.HashMap;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.spring.SpringBeanObjectFactory;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.LiveSQLMapper;
import org.hotrod.runtime.livesql.util.CastUtil;
import javax.annotation.PostConstruct;
import javax.sql.DataSource;
import org.hotrod.runtime.livesql.metadata.Column;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.queries.DeleteWherePhase;
import org.hotrod.runtime.livesql.queries.UpdateSetCompletePhase;
import org.hotrod.runtime.livesql.metadata.Name;
import org.hotrod.runtime.livesql.metadata.View;

import org.hotrod.runtime.livesql.queries.LiveSQLContext;
import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class PaymentDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  @Autowired
  private LiveSQLMapper liveSQLMapper;

  @Autowired
  private SpringBeanObjectFactory springBeanObjectFactory;

  @Autowired
  private DataSource dataSource;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
    this.sqlSession.getConfiguration().setObjectFactory(this.springBeanObjectFactory);
  }

  private LiveSQLContext context;

  @Value("${use.plain.jdbc:false}")
  private boolean usePlainJDBC;

  @PostConstruct
  public void initializeContext() {
    this.context = new LiveSQLContext(this.liveSQLDialect, this.sqlSession, this.liveSQLMapper, this.usePlainJDBC, this.dataSource);
  }

  // Row Parser

  public app.daos.reporting.PaymentVO parseRow(Map<String, Object> m) {
    return parseRow(m, null, null);
  }

  public app.daos.reporting.PaymentVO parseRow(Map<String, Object> m, String prefix) {
    return parseRow(m, prefix, null);
  }

  public app.daos.reporting.PaymentVO parseRow(Map<String, Object> m, String prefix, String suffix) {
    app.daos.reporting.PaymentVO mo = this.applicationContext.getBean(app.daos.reporting.PaymentVO.class);
    String p = prefix == null ? "": prefix;
    String s = suffix == null ? "": suffix;
    mo.setPaymentDate((java.sql.Date) m.get(p + "paymentDate" + s));
    mo.setInvoiceId(CastUtil.toInteger((Number) m.get(p + "invoiceId" + s)));
    mo.setAmount(CastUtil.toInteger((Number) m.get(p + "amount" + s)));
    return mo;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.reporting.PaymentVO> select(final app.daos.reporting.primitives.AbstractPaymentVO example, final PaymentOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.reporting.primitives.AbstractPaymentVO, PaymentOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("mappers.reporting.payment.selectByExample", dwo);
  }

  public Cursor<app.daos.reporting.PaymentVO> selectCursor(final app.daos.reporting.primitives.AbstractPaymentVO example, final PaymentOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.reporting.primitives.AbstractPaymentVO, PaymentOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.reporting.PaymentVO>(this.sqlSession.selectCursor("mappers.reporting.payment.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.reporting.PaymentVO> select(final PaymentDAO.PaymentTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.reporting.PaymentVO>(this.context, "mappers.reporting.payment.selectByCriteria",
        from, predicate);
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.reporting.PaymentVO insert(final app.daos.reporting.primitives.AbstractPaymentVO vo) {
    String id = "mappers.reporting.payment.insert";
    this.sqlSession.insert(id, vo);
    app.daos.reporting.PaymentVO mo = springBeanObjectFactory.create(app.daos.reporting.PaymentVO.class);
    mo.setPaymentDate(vo.getPaymentDate());
    mo.setInvoiceId(vo.getInvoiceId());
    mo.setAmount(vo.getAmount());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int update(final app.daos.reporting.primitives.AbstractPaymentVO example, final app.daos.reporting.primitives.AbstractPaymentVO updateValues) {
    UpdateByExampleDao<app.daos.reporting.primitives.AbstractPaymentVO> fvd = //
      new UpdateByExampleDao<app.daos.reporting.primitives.AbstractPaymentVO>(example, updateValues);
    return this.sqlSession.update("mappers.reporting.payment.updateByExample", fvd);
  }

  // update by criteria

  public UpdateSetCompletePhase update(final app.daos.reporting.primitives.AbstractPaymentVO updateValues, final PaymentDAO.PaymentTable tableOrView, final Predicate predicate) {
    Map<String, Object> values = new HashMap<>();
    if (updateValues.getPaymentDate() != null) values.put("\"payment_date\"", updateValues.getPaymentDate());
    if (updateValues.getInvoiceId() != null) values.put("\"invoice_id\"", updateValues.getInvoiceId());
    if (updateValues.getAmount() != null) values.put("\"amount\"", updateValues.getAmount());
    return new UpdateSetCompletePhase(this.context, "mappers.reporting.payment.updateByCriteria", tableOrView,  predicate, values);
  }


  // delete by example

  public int delete(final app.daos.reporting.primitives.AbstractPaymentVO example) {
    return this.sqlSession.delete("mappers.reporting.payment.deleteByExample", example);
  }

  // delete by criteria

  public DeleteWherePhase delete(final PaymentDAO.PaymentTable from, final Predicate predicate) {
    return new DeleteWherePhase(this.context, "mappers.reporting.payment.deleteByCriteria", from, predicate);
  }

  // DAO ordering

  public enum PaymentOrderBy implements OrderBy {

    PAYMENT_DATE("payment", "\"payment_date\"", true), //
    PAYMENT_DATE$DESC("payment", "\"payment_date\"", false), //
    INVOICE_ID("payment", "\"invoice_id\"", true), //
    INVOICE_ID$DESC("payment", "\"invoice_id\"", false), //
    AMOUNT("payment", "\"amount\"", true), //
    AMOUNT$DESC("payment", "\"amount\"", false);

    private PaymentOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static PaymentTable newTable() {
    return new PaymentTable();
  }

  public static PaymentTable newTable(final String alias) {
    return new PaymentTable(alias);
  }

  public static class PaymentTable extends Table {

    // Properties

    public final DateTimeColumn paymentDate = new DateTimeColumn(this, "payment_date", "paymentDate", "date", 13, 0, java.sql.Date.class, null, null);
    public final NumberColumn invoiceId = new NumberColumn(this, "invoice_id", "invoiceId", "int4", 10, 0, java.lang.Integer.class, null, null);
    public final NumberColumn amount = new NumberColumn(this, "amount", "amount", "int4", 10, 0, java.lang.Integer.class, null, null);

    // Getters

    public AllColumns star() {
      return new AllColumns(this.paymentDate, this.invoiceId, this.amount);
    }

    // Constructors

    PaymentTable() {
      super(null, null, Name.of("payment", false), "Table", null);
      initialize();
    }

    PaymentTable(final String alias) {
      super(null, null, Name.of("payment", false), "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      super.columns = new ArrayList<>();
      super.columns.add(this.paymentDate);
      super.columns.add(this.invoiceId);
      super.columns.add(this.amount);
    }

  }

}
