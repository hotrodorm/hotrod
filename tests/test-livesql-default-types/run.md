## Oracle Generate and Run

    mvn -P oracle hotrod:gen
    mvn -P oracle spring-boot:run 

## PostgreSQL Generate and Run

    mvn -P postgresql hotrod:gen
    mvn -P postgresql spring-boot:run
    
     