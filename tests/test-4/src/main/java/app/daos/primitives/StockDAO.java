// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractStockVO;
import app.daos.StockVO;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class StockDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // no select by PK generated, since the table does not have a PK.

  // select by unique indexes: no unique indexes found -- skipped

  // select by example

  public List<app.daos.StockVO> selectByExample(final app.daos.primitives.AbstractStockVO example, final StockOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractStockVO, StockOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("app.daos.primitives.stock.selectByExample", dwo);
  }

  public Cursor<app.daos.StockVO> selectByExampleCursor(final app.daos.primitives.AbstractStockVO example, final StockOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractStockVO, StockOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.StockVO>(this.sqlSession.selectCursor("app.daos.primitives.stock.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.StockVO> selectByCriteria(final StockDAO.StockTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.StockVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "app.daos.primitives.stock.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.StockVO insert(final app.daos.primitives.AbstractStockVO vo) {
    String id = "app.daos.primitives.stock.insert";
    this.sqlSession.insert(id, vo);
    app.daos.StockVO mo = new app.daos.StockVO();
    mo.setSku(vo.getSku());
    mo.setName(vo.getName());
    mo.setQuantity(vo.getQuantity());
    return mo;
  }

  // no update by PK generated, since the table does not have a PK.

  // no delete by PK generated, since the table does not have a PK.

  // update by example

  public int updateByExample(final app.daos.primitives.AbstractStockVO example, final app.daos.primitives.AbstractStockVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractStockVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractStockVO>(example, updateValues);
    return this.sqlSession.update("app.daos.primitives.stock.updateByExample", fvd);
  }

  // delete by example

  public int deleteByExample(final app.daos.primitives.AbstractStockVO example) {
    return this.sqlSession.delete("app.daos.primitives.stock.deleteByExample", example);
  }

  // DAO ordering

  public enum StockOrderBy implements OrderBy {

    SKU("public.stock", "sku", true), //
    SKU$DESC("public.stock", "sku", false), //
    SKU$CASEINSENSITIVE("public.stock", "lower(sku)", true), //
    SKU$CASEINSENSITIVE_STABLE_FORWARD("public.stock", "lower(sku), sku", true), //
    SKU$CASEINSENSITIVE_STABLE_REVERSE("public.stock", "lower(sku), sku", false), //
    SKU$DESC_CASEINSENSITIVE("public.stock", "lower(sku)", false), //
    SKU$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.stock", "lower(sku), sku", false), //
    SKU$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.stock", "lower(sku), sku", true), //
    NAME("public.stock", "name", true), //
    NAME$DESC("public.stock", "name", false), //
    NAME$CASEINSENSITIVE("public.stock", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("public.stock", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("public.stock", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("public.stock", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("public.stock", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("public.stock", "lower(name), name", true), //
    QUANTITY("public.stock", "quantity", true), //
    QUANTITY$DESC("public.stock", "quantity", false);

    private StockOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static StockTable newTable() {
    return new StockTable();
  }

  public static StockTable newTable(final String alias) {
    return new StockTable(alias);
  }

  public static class StockTable extends Table {

    // Properties

    public StringColumn sku;
    public StringColumn name;
    public NumberColumn quantity;

    // Getters

    public AllColumns star() {
      return new AllColumns(this);
    }

    // Constructors

    StockTable() {
      super(null, "PUBLIC", "STOCK", "Table", null);
      initialize();
    }

    StockTable(final String alias) {
      super(null, "PUBLIC", "STOCK", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.sku = new StringColumn(this, "SKU", "sku");
      this.name = new StringColumn(this, "NAME", "name");
      this.quantity = new NumberColumn(this, "QUANTITY", "quantity");
    }

  }

}
