// Autogenerated by HotRod -- Do not edit.

package app.daos.primitives;

import java.io.Serializable;
import java.util.List;

import org.apache.ibatis.session.SqlSession;
import org.hotrod.runtime.cursors.Cursor;
import org.hotrod.runtime.livesql.queries.select.MyBatisCursor;

import org.hotrod.runtime.interfaces.DaoWithOrder;
import org.hotrod.runtime.interfaces.UpdateByExampleDao;
import org.hotrod.runtime.interfaces.OrderBy;

import app.daos.primitives.AbstractCodeVO;
import app.daos.CodeVO;

import org.hotrod.runtime.livesql.expressions.ResultSetColumn;
import org.hotrod.runtime.livesql.dialects.LiveSQLDialect;
import org.hotrod.runtime.livesql.metadata.NumberColumn;
import org.hotrod.runtime.livesql.metadata.StringColumn;
import org.hotrod.runtime.livesql.metadata.DateTimeColumn;
import org.hotrod.runtime.livesql.metadata.BooleanColumn;
import org.hotrod.runtime.livesql.metadata.ByteArrayColumn;
import org.hotrod.runtime.livesql.metadata.ObjectColumn;
import org.hotrod.runtime.livesql.metadata.Table;
import org.hotrod.runtime.livesql.expressions.predicates.Predicate;
import org.hotrod.runtime.livesql.metadata.AllColumns;
import org.hotrod.runtime.livesql.queries.select.CriteriaWherePhase;
import org.hotrod.runtime.livesql.metadata.View;

import org.springframework.stereotype.Component;
import org.springframework.beans.BeansException;
import org.springframework.context.annotation.Lazy;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.ApplicationContext;
import org.springframework.context.ApplicationContextAware;

@Component
public class CodeDAO implements Serializable, ApplicationContextAware {

  private static final long serialVersionUID = 1L;

  @Autowired
  private SqlSession sqlSession;

  @Autowired
  private LiveSQLDialect liveSQLDialect;

  private ApplicationContext applicationContext;

  @Override
  public void setApplicationContext(final ApplicationContext applicationContext) throws BeansException {
    this.applicationContext = applicationContext;
  }

  // select by primary key

  public app.daos.CodeVO selectByPK(final java.lang.Integer id) {
    if (id == null)
      return null;
    app.daos.CodeVO vo = new app.daos.CodeVO();
    vo.setId(id);
    return this.sqlSession.selectOne("app.daos.primitives.code.selectByPK", vo);
  }

  // select by unique indexes: no unique indexes found (besides the PK) -- skipped

  // select by example

  public List<app.daos.CodeVO> selectByExample(final app.daos.primitives.AbstractCodeVO example, final CodeOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractCodeVO, CodeOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return this.sqlSession.selectList("app.daos.primitives.code.selectByExample", dwo);
  }

  public Cursor<app.daos.CodeVO> selectByExampleCursor(final app.daos.primitives.AbstractCodeVO example, final CodeOrderBy... orderBies)
      {
    DaoWithOrder<app.daos.primitives.AbstractCodeVO, CodeOrderBy> dwo = //
        new DaoWithOrder<>(example, orderBies);
    return new MyBatisCursor<app.daos.CodeVO>(this.sqlSession.selectCursor("app.daos.primitives.code.selectByExample", dwo));
  }

  // select by criteria

  public CriteriaWherePhase<app.daos.CodeVO> selectByCriteria(final CodeDAO.CodeTable from,
      final Predicate predicate) {
    return new CriteriaWherePhase<app.daos.CodeVO>(from, this.liveSQLDialect, this.sqlSession,
        predicate, "app.daos.primitives.code.selectByCriteria");
  }

  // select parent(s) by FKs: no imported keys found -- skipped

  // select children by FKs: no exported FKs found -- skipped

  // insert

  public app.daos.CodeVO insert(final app.daos.primitives.AbstractCodeVO vo) {
    String id = "app.daos.primitives.code.insert";
    this.sqlSession.insert(id, vo);
    app.daos.CodeVO mo = new app.daos.CodeVO();
    mo.setId(vo.getId());
    mo.setName(vo.getName());
    return mo;
  }

  // update by PK

  public int update(final app.daos.CodeVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.update("app.daos.primitives.code.updateByPK", vo);
  }

  // delete by PK

  public int delete(final app.daos.CodeVO vo) {
    if (vo.id == null) return 0;
    return this.sqlSession.delete("app.daos.primitives.code.deleteByPK", vo);
  }

  // update by example

  public int updateByExample(final app.daos.primitives.AbstractCodeVO example, final app.daos.primitives.AbstractCodeVO updateValues) {
    UpdateByExampleDao<app.daos.primitives.AbstractCodeVO> fvd = //
      new UpdateByExampleDao<app.daos.primitives.AbstractCodeVO>(example, updateValues);
    return this.sqlSession.update("app.daos.primitives.code.updateByExample", fvd);
  }

  // delete by example

  public int deleteByExample(final app.daos.primitives.AbstractCodeVO example) {
    return this.sqlSession.delete("app.daos.primitives.code.deleteByExample", example);
  }

  // DAO ordering

  public enum CodeOrderBy implements OrderBy {

    ID("schema3.code", "id", true), //
    ID$DESC("schema3.code", "id", false), //
    NAME("schema3.code", "name", true), //
    NAME$DESC("schema3.code", "name", false), //
    NAME$CASEINSENSITIVE("schema3.code", "lower(name)", true), //
    NAME$CASEINSENSITIVE_STABLE_FORWARD("schema3.code", "lower(name), name", true), //
    NAME$CASEINSENSITIVE_STABLE_REVERSE("schema3.code", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE("schema3.code", "lower(name)", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_FORWARD("schema3.code", "lower(name), name", false), //
    NAME$DESC_CASEINSENSITIVE_STABLE_REVERSE("schema3.code", "lower(name), name", true);

    private CodeOrderBy(final String tableName, final String columnName,
        boolean ascending) {
      this.tableName = tableName;
      this.columnName = columnName;
      this.ascending = ascending;
    }

    private String tableName;
    private String columnName;
    private boolean ascending;

    public String getTableName() {
      return this.tableName;
    }

    public String getColumnName() {
      return this.columnName;
    }

    public boolean isAscending() {
      return this.ascending;
    }

  }

  // Database Table metadata

  public static CodeTable newTable() {
    return new CodeTable();
  }

  public static CodeTable newTable(final String alias) {
    return new CodeTable(alias);
  }

  public static class CodeTable extends Table {

    // Properties

    public NumberColumn id;
    public StringColumn name;

    // Getters

    public AllColumns star() {
      return new AllColumns(this);
    }

    // Constructors

    CodeTable() {
      super(null, "SCHEMA3", "CODE", "Table", null);
      initialize();
    }

    CodeTable(final String alias) {
      super(null, "SCHEMA3", "CODE", "Table", alias);
      initialize();
    }

    // Initialization

    private void initialize() {
      this.id = new NumberColumn(this, "ID", "id");
      this.name = new StringColumn(this, "NAME", "name");
    }

  }

}
